extends Resource
## One recovered visual language, without another material or power system.
## Common resources carry it only where the native v4 field has a clear signal.
@export var common_influence_threshold := 0.32
## Imported land/craft metal stays pale and worn, with no threat-colour glow.
@export var creature_inlay_colour := Color("b8b6a3")
@export var creature_inlay_dark := Color("626b64")
## Fraction of the shared one-metre attachment used on existing body parts.
@export var creature_inlay_scale := 0.34
@export var resource_inlay_scale := 0.48
## Hand-worked housings mute the exposed land metal and its faint residual light.
@export var craft_inlay_tint := Color("8e9788")
@export var craft_inlay_roughness := 0.82
@export var craft_inlay_emission_scale := 0.25
## Dry skin, cloth and mineral plates keep the study's restrained highlights.
@export var actor_roughness := 0.93
@export var design_purpose: Dictionary = {}
