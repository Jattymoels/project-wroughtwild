# Preserve the exact prompt bytes recorded by the image provenance manifest.
*-prompt.txt text eol=lf
# Surface reports retain an exact recipe hash across Windows checkouts.
surface-study.json text eol=lf
# ART-06C was built from CRLF bytes; reproduce that checkout hash on every OS.
lifeline-study.json text eol=crlf
