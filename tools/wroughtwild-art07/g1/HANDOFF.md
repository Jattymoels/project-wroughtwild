# ART-07G1 retained route and home pilot

Technically checked integration, with owner visual acceptance pending. G2 has
not started. All runtime game/data/native sources are pinned to
`6bb2e044dcd0bf1788896aa2c19cdf56fee93522`. This is a copied adoption pilot; the
normal checkout and its saves are separate.

Run `launch.ps1` in PowerShell to open the paid retained LF3 seed-77 home.
The package includes the already installed Godot 4.5-stable executable.
Use `launch.ps1 -Import` for a clean source reimport,
`launch.ps1 -Renderer gl_compatibility` for Compatibility,
`launch.ps1 -Baseline` for the native art-disabled comparison, and
`launch.ps1 -Smoke` for the checked 120-frame sheltered-home launch.
The mutable save is the sibling `<package-name>-user/ART07G1/g1-play.json`.
The embedded `game/g1/paid-home.json` remains the original paid checkpoint.

WASD/mouse move and look; E interacts; C crafts; B enters building; Tab opens
the shape/kit picker; Q selects material; R rotates; LMB works/places; I opens
inventory; H shows help; F5/F9 save/load the isolated play save. The player starts
inside the completed timber home. Three paid stations stand beside the existing
Red-source trail. No inspection stock is added to this playable save.

The paid harness gathered and crafted through finite native sources and ordinary
transactions. It paid 188 timber for the home and retained 17 timber in its chest.
The paired art-on/art-off run walked the existing 135.078 m source route with
the native controller and matched all recorded ownership/geography snapshots.
Gathering travel and target selection were paced by the harness; this is not an
uncut human first-hour playthrough. Separate labelled fixtures use explicit
inspection stock to test all 273 legal shape/material pairs and device cases.

`evidence/validation.json` records successful logs and exact commands;
`evidence/benchmark-comparison.json` records all matched camera/light samples,
including total loaded texture/buffer/video costs and complete live-scene mesh
counts. `evidence/paid-fingerprints.json` and `evidence/probes.json` contain
baseline comparisons. `lineage/` contains the exact 24-input index, sealed source
manifests, published receipts and full before/after hash-verification reports.
`masters/` holds 30 unchanged packed source masters. `blender/` holds the packed
static inspection assembly, actual material/clay renders and reopen report.
`recipes/g1/README.md` explains reconstruction from the pinned repository.
`diagnostics/` keeps failed attempts separate from successful validation.

Known limits are deliberate review findings, not owner acceptance:

- The inherited B1 native tree fit is visibly narrow; distant native canopy
  presentation remains in use outside streamed live resources.
- Existing cover envelopes/counts are retained. Only matching B2 ground-cover
  roles use the B4 rooted sway; unmatched supporting assets remain available
  for labelled inspection rather than gaining new generated anchors.
- Generic building forms use published face maps with metric triplanar mapping.
  Bespoke end-grain/edge material roles remain in the authored station/home
  assets; generic per-face finishing still needs review.
- The native chest intersects a flat floor by 0.15 m. Bodies and placement rules
  are unchanged. C5 raised ore meshes remain fallback-only; normal faceted ore
  keeps the native thin ribbon.
- Source adapters can retain hidden work stages and unused LOD resources. The
  measured texture/geometry costs are substantial. RTX 5090 measurements are
  not a lower-end hardware or global world-adoption acceptance claim.
- Godot's dummy renderer logs a shutdown null-material error for the D3/full
  catalogue assembly. Those runs are rejected diagnostics; unchanged assertions
  pass under the supported renderers. Compatibility retains the native SSAO
  unsupported warning. Unit rejection cases and some source fixture cleanup
  produce documented expected warnings.

The F4 restore refresh now reads the native pocket ledger synchronously before
world recovery resumes: an exhausted pocket cannot briefly show full stock.
Fractional escrow, native drive, source IDs, geography, body dimensions and save
schema remain authoritative. F1 requests create no drive. F5 presentation reads
actual counters/progress and never owns quantities or replays load events.

Verify delivery bytes with `python recipes/g1/manifest.py verify <package>`.
Generated `.godot` caches, UID sidecars and Blender backup files are excluded
from the seal. Main publication belongs to the dedicated publisher.
