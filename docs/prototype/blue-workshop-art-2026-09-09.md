# ART-04C — Blue source to delay

**Technical handoff delivered and visually approved — 9 September 2026.** After reviewing the
White handoff in chat, the owner answered “Yep approved, next!”. White is visually
approved. This selects Blue only; Green and ART-05 remain later slices.

## Outcome, affected systems and boundaries

Finish one existing Blue layered fracture, three representative recovered Blue
Flakes and the existing Blue Delay. Nested mineral lips and narrow blue light in
held fractures connect the roles. Preserve the source and device bodies, signal
endpoint, native ownership and three-second request contract. No new gameplay,
recipe, Frost effect, save schema, geography or ordinary-world adoption.

[LF-2A](living-frontier-wave2-2026-09-08.md#lf-2a--selected-contract-before-implementation)
owns extraction, rare claims, kit costs and pending requests. The
[asset roadmap](leyline-asset-roadmap-2026-09-09.md) and accepted D-013/D-030
own visual direction; D-010/D-017/D-033 preserve behavior and placements.
[White](white-workshop-art-2026-09-09.md) establishes the handoff procedure.

## Small plan and assumptions

1. Record White approval, inspect published Blue state and preserve concurrent work.
2. Use built-in ImageGen and approved local TRELLIS for an original layered host;
   inspect all sides, then finish scars, closed flakes and a fitted delay in Blender.
3. Bind an isolated Godot review to a copied paid Blue checkpoint. Pending light
   follows actual remaining delay; explicit pause, blocked output and inactive time
   retain that position. Release consumes one request even if its receiver refuses.
4. Check native ownership/restart, body/port fit, reopened exports and editable
   source; capture both renderers and measure the isolated scene honestly.
5. Package provenance and recipes, show actual finished pictures in chat, update
   the queue, commit and push the scoped checked handoff.

Three displayed flakes represent a native claim or carried stack, never extra
inventory. Existing signal/cargo and grove props are context. Review layout is
not new generated geography. No material gameplay decision is unresolved;
visual approval follows technical delivery.

## Delivered result

One original built-in ImageGen input and local TRELLIS v0.6.0 source preserve
the broad, asymmetric slate layers. Generation used seed 42, 1024 resolution,
2048 textures and the recorded model/runtime revisions, completing in 148.2 s.
The unchanged raw source has 287,422 triangles; its SHA-256 is
`6165314bec6143e66a1fc4f7bd32ef9d582ea743bb854fc9819f25c8cdd83eb2`.
The input SHA-256 is
`a7f54c3a29fb681f3f922339271fc3a787b867fb161ca4400003d6a715347329`.
The exact prompt, input, raw export and generation log are retained.

Blender finishing retains the original texture charts and mineral planes,
corrects face orientation and adds up to 3 mm of recessed damage with separate
core/damage/travel masks and tangent normals. Three solid flakes come from actual
scar-bearing surface cuts. Their own 1k atlases use closest-source triangles and
barycentric UVs within each triangle's chart; newly exposed faces infer mineral
appearance. Independent tangent normals avoid transferring the source UV frame
incorrectly. All three flakes remain closed after runtime export/reimport.

The delay has twin timber supports, a forged cradle, three pairs of retaining
lips and the unchanged top terminal. The same slate is captured vertically.
The first fit failed because the imported object retained quaternion rotation
mode; explicit Euler mode corrected the intended orientation. A subsequent
seating adjustment closes the small cradle gap. Exported geometry fits the
unchanged body and the captured stone makes measured contact with its cradle.

Natural source light lingers locally over a 4.5-second cosmetic cycle. Device
light instead follows actual native elapsed delay: explicit pause, blocked output
and inactive time retain its position. Expiry gives one 0.65-second release,
including a receiver refusal. Cancel, rewire and historical counters do not play
a release. Review pause freezes all clocks. The held crest is checked in actual
rendered pixels, not only shader parameters.

The isolated review preserves the paid lever/White/Blue/drum/landing relative
positions, three native signal spans (4.1231, 4.1231 and 5.6569 m), 14 m cargo
span, actual signal rays and the existing 0.3 m basket sweep. White and the
older lever/winch/landing/grove are context. Explicit material binding also
corrects the context White GLB's fallback interpretation; no White game asset
or gameplay behavior is changed.

Local handoff: `build/workshop-blue/blue-handoff/`, with launcher, packed editable
source, nine runtime exports/maps, standalone review, both-renderer captures,
native evidence, recipes and raw provenance. All **338 files / 875,931,572 bytes**
passed the SHA-256 manifest check. Generated meshes, blends, DLLs, imports and
caches stay under ignored `build/`. [Finished pictures and evidence](../art/leyline-studies/2026-09-09/workshop-blue/README.md);
[reproduction recipe](../../tools/wroughtwild-blue/README.md).

## Checks

The isolated DLL remains the hash-verified ART-04 baseline
`f7253d20ed1dbeee743219190bdaccac60921c1c`; concurrent Living Frontier work is
not compiled into this study. The copied paid checkpoint is
`build/lf2/published-a/lf2-pending.json`, SHA-256
`f83329eb2f2a8054dd33c7b9b726c7f4000097b67d6f12a41822a0354d1ea9d8`.
It owns 14 carried Blue Flakes, source lot 1/8, ten wood, one winding, two previous
trips and a request elapsed exactly 0.8 of 3 seconds. Normal saves and existing
playtests were not used or changed.

- **90 native + 16 fresh-process checks:** exact saved delay, duplicate refusal,
  global/explicit/inactive pause, blocked output/cargo, single payment, no replay
  after later winding, cancellation and upstream/downstream rewiring. Original
  cargo arrives and collects once. Raw/partial/full/rare-only source claims,
  source exhaustion and blocked reformation preserve native ownership on restart.
- **119 asset checks:** nine reopened exports, all detail-level envelopes,
  physical cradle contact, positive closed flake volumes after UV-seam welding,
  nonempty expected maps including every flake's core, and packed editable reopen.
- **548 visual/state checks per renderer:** 97 captures each in Forward+ and
  Compatibility. Actual imported shaders, source/claim/pack state, held requests,
  physical obstruction, cancellation, paid arrival and failed releases agree.
  Matched day/shade, emission-off and detail-level views use no bloom.
- **293 evidence checks:** exact recorded balances, real scar-pixel changes with
  emission or native progress, identical held/paused core pixels in both renderers,
  and all 64 original frames across the lossless WebP's complete 8,000 ms timeline.
  One duplicate frame is duration-merged; the clip is one paid delivery.

The rare-outcome test is separate and labelled: it consumes test-carried flakes
to clear capacity and advances actual formation/fixed outcomes. It grants no
inventory and edits no outcome. It proves rare-only ownership/visibility, not
normal extraction pacing. The runtime review exposes the rare count and separate
collection action; no fourth new Catalyst asset is introduced.

## Measured limits and next gate

| Runtime role | Near triangles | Mid | Far | Material surfaces |
| --- | ---: | ---: | ---: | ---: |
| Blue source | 35,999 | 15,999 | 7,999 | 1 |
| Delay including mineral | 38,651 | 18,651 | 10,651 | 2 |
| Recovered flakes | 192 / 134 / 176 | Same meshes | Same meshes | 1 each |

The source remains an exterior mesh, not a certified geological solid. An
additional near-mesh UV-weld inspection found 44 boundary edges and 193
non-manifold edges, with no zero-area faces. Only the flakes carry the closed
volume guarantee. The lower far level succeeds here, but these are review
budgets, not a final populated-world budget.

At 1600 × 1000 on the RTX 5090, six isolated Forward+ day/shade cases measured
GPU medians of **0.333–0.556 ms**, CPU render medians **0.122–0.142 ms**, and
106–150 draw calls. GPU p95 was **0.381–1.661 ms**, including the higher first
source-view sample; that outlier is retained. Texture residency was
**742,393,344 bytes (708 MiB)**, including all loaded levels, repeated fallback
maps and context. This is not normal-world FPS or an accepted production texture
budget. Cooking/deduplication, lower-spec tests, streaming, first-person placement
and actual world adoption remain ART-05 work.

[`blue.json`](../../tools/wroughtwild-blue/blue.json) explains presentation tuning.
No native gameplay tuning changed. Use the supplied explicit Godot material
binding; GLB fallbacks do not reliably interpret the authored shader nodes.
Offline sandbox certificate-store warnings remain logged, while all other
engine/script/shader/assertion errors fail. UTF-8/newline errors caught in the
first derived review were corrected before final captures. Blender resources
and Godot APPDATA are isolated.

The owner approved Blue's actual pictures in chat: “Yea looks awesome, continue”.
The next selected slice is [ART-04D Green](green-workshop-art-2026-09-09.md).
ART-05 remains a separate later slice.
