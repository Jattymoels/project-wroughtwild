# ART-07 repair publication and dispatch

Copy into the serial coordinating task after a repair worker reports completion.

```text
Coordinate publication of completed ART-07 repair candidates only.
Owner checkout: C:/Users/Matty/Dev/project-wroughtwild.
Read the current owner AGENTS.md Prototype work limits FIRST. This owner's later
instruction supersedes exhaustive steps below: 10 minutes of review/verification,
up to three focused checks, one renderer, no routine rerun of worker evidence or
full parent rehash/copy/seal. For a documentation publication, check only its diff
and the affected dispatch/link consistency. Reuse existing valid packages. Stop
with explicit limits rather than expanding verification; request additional time
only for a specific necessary risk and check. R9 was stopped by the owner; publish
its available conclusions without launching further tests or requiring a new seal.
Read AGENTS.md in its required order, then docs/prototype/art07-repairs/2026-09-14/
README.md, COMMON.md, SETUP.md, plan.json, inputs.json, workspaces.json and deliveries.json.

Inspect actual main/status/history, origin and worker receipt/branch. Preserve all
unrelated captures/saves/worktrees. The owner-approved destination is origin/main at
https://github.com/Jattymoels/project-wroughtwild.git; standing permission covers
ordinary checked commits and non-force pushes. No resets, cleans, force pushes,
other-task messages or acknowledgement exchanges.

For ONE completed candidate at a time:
1. Resolve the worker's exact implementation and receipt commits from actual history.
   Verify exclusive owned paths, original/dependency hashes, package seal and absolute
   local accessibility. A prepared workspace or candidate branch is not a delivery.
2. Inspect the full diff, changes.json and actual model evidence. Reproduce its
   meaningful verifier and fresh-package/restart checks. Keep GPU work serialized.
   Preserve open visual/cost findings and blocked decisions; do not implement repairs.
3. Integrate exact checked commits by fast-forward or scoped cherry-pick according to
   real ancestry. Preserve unrelated work. Do not merge a whole competing runtime;
   R8 owns the explicit combined file/function/settings reconciliation.
4. Update only this receipt and the shared repair delivery/setup/queue records.
   In deliveries.json record status=published, the actual integrated source_commit,
   sibling JSON receipt path and verified package object (path, manifest_sha256,
   files, bytes). The worker receipt must be ready_for_integration. Blocked findings
   stay out of the published-ready index; report their concrete decision request.
5. Validate the repair pack with tools/wroughtwild-art07-repairs/dispatch.py and the
   original session_plan/catalogue/verify_art validators; check scope/whitespace and
   links. Commit publication notes and push to the approved origin/main. Verify the
   remote tip; report local integration and remote publication separately.
6. Release a new wave only when all plan.json start_after/dependency candidates are
   checked and published. Use workspace.py create with the actual inspected published
   main SHA to prepare the next directories when the owner requests setup/dispatch.
   Provide complete ready prompts and real paths; do not launch tasks automatically.

R1–R4 form the first wave; R5/R6/R7 follow; R8 integrates all seven; R9 independently
reviews R8 in a task that implemented none of R1–R8. R2 improvement must be remeasured
after final integration. Report each G2 finding as open, partial or technically
resolved with evidence, keeping owner visual acceptance and target hardware separate.
The game/data/native baseline remains 6bb2e044dcd0bf1788896aa2c19cdf56fee93522.
All 273 combinations including octagonal/chamfer/triangle uses, native bodies,
stock/geography and paid save ownership remain constraints. No ordinary-world
rollout, new geography/body decision or ART-06C rig adoption is implied by completion.

If platform automatic approval review rejects a publication, state the exact action
and reason; do not retry by another route. Otherwise continue under standing scope.
Finish once with the actual commit/push outcome, remaining limits and next ready
prompts. Do not create monitors or restart completed workers for status notices.
```
