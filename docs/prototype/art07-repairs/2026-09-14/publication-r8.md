# ART-07R8 publication — 14 September 2026

R8 combines the seven published repair candidates into one reproducible retained-world
package. It reconciles shared material/resource hooks, preserves fitted tree and chest
geometry, applies exact texture reuse to final assets, and keeps native gameplay,
saved ownership, all 273 building pairs and the 16 original actors pinned.

The publisher verified the entire 9,890-file, 13,437,587,547-byte seal and all common/
repair dependency payloads, all 49 implementation files and the exact source/receipt
ancestry. Reproduced source checks confirm 807 exact image aliases, 261 image-reference-
only model derivatives and unchanged data, engine, native DLL and save-manager bytes.
The original acceptance verifier reproduces 191 required successful jobs. Complete
paid/probe comparisons and the camera/traversal cost calculations also reproduce;
259 referenced image/motion hashes match. These are reproduced evidence calculations,
separate from the fresh publisher engine runs below.

## Fresh publisher checks

Runtime: `D:\project-wroughtwild-art07-r8\build\art07-repairs\r8\v04\runtime`. All 5,673 declared runtime files and 47 copied packed
masters retain their expected hashes after execution. Thirteen guarded jobs pass:

| Job | Whole-process seconds | Exit |
| --- | ---: | ---: |
| publisher-import | 78.82 | 0 |
| publisher-paid-art-forward_plus-final | 97.19 | 0 |
| publisher-paid-restart-art-forward_plus-final | 61.21 | 0 |
| publisher-f4-flow-forward_plus | 77.94 | 0 |
| publisher-f4-fractional-forward_plus | 57.44 | 0 |
| publisher-f4-exhausted-forward_plus | 56.87 | 0 |
| publisher-catalogue-gl_compatibility | 6.94 | 0 |
| publisher-comfort-live-gl_compatibility | 61.95 | 0 |
| publisher-reopen-r1-02 | 21.31 | 0 |
| publisher-reopen-r3-02 | 1.66 | 0 |
| publisher-reopen-r5 | 113.41 | 0 |
| publisher-reopen-r6 | 19.14 | 0 |
| publisher-reopen-r7 | 1.55 | 0 |

The paid flow passes 865 unchanged assertions and walks 135.078021510504 m; its complete
initial/paid/final native fingerprints match the worker. The original comparison omits
only generated station scene names, never persistent keys, positions or ownership.
A separate paid restore passes eight assertions. F4 retains its original 227 flow,
32 fractional-restore and 29 exhausted-restore checks. Compatibility checks all 273
building pairs and observes 120 rendered player frames with visible mouse and the
actual no-focus flag. Five background Blender processes reopen selected sources.

The first R1 reopen stopped at its fresh-output guard because the copied source
tree included historical reopen output. A new input root with byte-identical models
and no previous output passed the original verifier. The failed log and exact retry
input hashes remain in the publication JSON; no assertion or model was changed.
The publisher also mistakenly precreated R3/R5 output directories. R3's same
fresh-output guard stopped before asset checks; R5 had not run. Both were given
new absent destinations and passed unchanged verifiers. The R3 failure is retained.

All rendered publisher jobs use the verified test mouse-capture opt-out and disposable
startup no-focus override. Normal play excludes that override and retains its original
capture branch. Blender helpers disable bytecode before imports; all original source
and native assertions remain unchanged. No desktop pointer movement is automated.

## Outcome and remaining limits

The actual Forward+ G1/R8 home pair and Compatibility bog-oak view show broader crowns
and the combined materials, while heavy limbs, dark shade and sparse ground cover remain.
Owner visual acceptance is pending. The original current-machine measurements show
34.08%/33.61% lower median setup in Forward+/Compatibility and lower texture memory,
but higher settled-frame medians and approximately 451–453 ms traversal stalls.
These benchmarks were recalculated from verified samples, not rerun by the publisher.

Raised ore geometry/physical recess and continuous understorey remain unmet pending
the documented body/geography choices. Chest clearances, an unexplained earlier paid
walk stall, bounded reload buffer growth, long-session stability and target hardware
remain open exactly as recorded in the [worker receipt](receipts/r8.md).

The exact implementation `67865cf44739f9f9f779c36be49042dd77342d5e` and receipt `7a5bc56936f1565cd94c450e6ca36ae1ccdbde15` were fast-forwarded onto main.
Publication records accompany the ordinary approved origin/main push; its remote tip
is checked separately. All 49 unrelated owner checkout entries remain untouched.

R9 is the next gate: a new independent task that implemented none of R1–R8 must assess
each of the seven original findings, including visuals and costs. Its actual setup is
recorded separately after publication. This does not authorize ordinary-world rollout.
The coordinator corrected the shared R9 preparation path for R8's nested runtime
layout. It requires the final runtime map to match the sealed manifest exactly and
hashes every copied file. The verified no-focus override is copied separately for
automated tests and smoke commands opt out of mouse capture. Parent setup paths and
sealed support files remain unchanged; this coordinator fix is outside R8's source commit.

See [full publication evidence](publication-r8.json), [checks](publication-r8-checks.json),
[deliveries](deliveries.json), [setup](SETUP.md) and the [R9 input](../../../art/leyline-studies/2026-09-14/art07-repairs/r8/R9-input.json).
