# ART-07R5 — correct chest visual seating

Status: ready_for_integration. Owner visual acceptance: pending.
Ordinary-world rollout: outside scope.

## Source and scope

Worktree `D:/project-wroughtwild-art07-r5`, branch `codex/art07-r5`, inspected
published checkout `a3aab16f7220d6ccc37cb17a54a164a84bf524b6`. Runtime/game/data/native
pin remains `6bb2e044dcd0bf1788896aa2c19cdf56fee93522`. R1-R4 published source and receipt commits
were verified as ancestors and their real packages rehashed before work and again
at completion. Their exact identities appear in `r5.json`; no predecessor delta is
consumed. Original G1/G2/E3 packages and owner worktree/saves remain untouched.

Common runtime source: `D:/project-wroughtwild-art07-g2/build/art07/g2/v01/sealed`,
manifest `fd5c592ef52185cc7d0737840e41af09dbb5bcea36b719539931e156f42865cd`.
Packed E3 source: `C:/Users/Matty/Dev/project-wroughtwild/build/art07/e3/worktree/build/art07/e3/v10/handoff`,
manifest `b3a06254c13e48c3c60f613ec161dacb3c4a8e3b554036d25ff1dcb21cda6bc7`.
Native DLL remains `0e7c4956b6c870f5879e78f54c5830509b7ae37913055543cc3a6d451298e0e1`.

Only the four permitted R5 Git locations are owned: `tools/wroughtwild-art07-repairs/r5/`,
`docs/art/leyline-studies/2026-09-14/art07-repairs/r5/`, and these two receipt files.
Actual baseline-relative runtime changes are eight `game/assets/authored/e3/chest_*_body.glb`
files plus their eight measured rows in `game/e3/geometry.json`. JSON serialization
adds a final newline; other rows retain their values. All nine before/after hashes,
bytes, replacement paths and purposes are in `changes.json` and the sibling receipt.
Four `game/r5/` review files are separately marked fixture-only. R2 texture-storage
work overlaps the replaced GLBs; R8 must reconcile that work against this common
baseline. This package is not permission to overwrite other repairs wholesale.

## Result and checks

The old base intersects the nominal plane by 0.150 m; actual ordinary/fine slab
intersections measure 0.275 / 0.2125 m. The authored cabinet underside now meets
ordinary slabs (gap 0), clears fine slabs (+0.0625 m), and stands on inset feet on
flat ground (+0.125 m cabinet clearance). Feet themselves contact nominal ground;
ordinary/fine slabs conceal 0.125 / 0.0625 m of them. This is deliberately recorded
mesh penetration beneath the visible cabinet. No floor is hidden or frame cropped
to claim a fix. Full selected before/after PNGs and actual opening GIFs are in
`evidence/selected/`; provenance preserves paths, timestamps, dimensions and hashes.

All 24 selected source/import/runtime jobs exit 0. Candidate native checks total
1,280 across both renderers: 294 capture + 32 fresh restore + 249 benchmark-preflight
+ 65 retained-world assertions per renderer. Baseline totals 1,080. The full audit
compares 24 fixture cases and eight retained-world cases; saved ownership/block
records, body/pose/hinge and support samples remain exact. Complete retained terrain
and source/work/machine ledgers remain unchanged. Unpaid and duplicate placement
refuse without spending; successful placement pays and owns one store. Actual E
interaction, deposit 17 wood + 23 stone, withdrawal 3 stone, 960 capacity, native lid
opening/closing and two fresh-process loads pass. Native and all unrelated runtime
bytes preserve the 273-pair catalogue; only affected chest flows are replayed here.

A fresh packed reopen checks 42 packed images, eight closed/open families and 16
independently imported GLBs. Lids and embedded texture images are byte-exact to E3.
Changed source master: `source-candidate/r5_chest.blend`, SHA-256
`55392f9725c07e8005facdefbc22f4534bd8fb4df78de6a67a22a06347a84382`. Original master/exports remain under
`source-original/`. Selected source views and measurements are `evidence/reopen/`;
the earlier cropped camera run and its executed script remain separately labelled.
The full runtime and source assets retain E3's existing part/LOD selection and maps.

Geometry, fit controls, their plain-language purposes, render controls, retained
clearance limits and all real measurements are documented in
`tools/README.md`, `tools/fit.json` and `evidence/selected/README.md` in the seal.
The eight controls are source base -0.500, cabinet base -0.225, fixed upper region
+0.060, foot bottom -0.350, foot X/depth centres +/-0.400 / +/-0.290, width 0.080,
and joint overlap 0.020 m. Native physical/storage dimensions remain 1 x 0.7 x 0.8 m;
visible dimensions become 1 x 0.55 x 0.8 m. Cosmetic voids retain the solid native
collider. Hinge remains Godot (0, 0.09, -0.354), -78 degrees, 3.8 rad/s.

Current-machine costs: RTX 5090 / Ryzen 9 9950X3D / NVIDIA 591.86, Windows 11 build
26200, 33,446,744,064 bytes RAM, Godot 4.5, 1440 x 900, 4x MSAA, vsync off.
Each separate benchmark settles 120 frames then samples 300 process-frame intervals.
No benchmark competitor was observed. Forward+ p50/p95/worst: baseline
0.464/0.834/1.232 ms, candidate 0.467/0.831/1.230 ms. Compatibility:
0.876/1.264/1.770 -> 0.867/1.353/1.752 ms. Draws stay 170 / 234 and texture bytes
459915776 / 272763190; frame primitives rise 231458 -> 247010. Each closed source
chest rises 6336 -> 6768 triangles (+432 / 6.82%). These are one-run current-machine
measurements, not minimum-hardware or statistical acceptance.

Selected logs preserve the inherited SSAO-only-in-Forward+ warning in Compatibility
smoke and retained-terrain runs; no other warning/error/leak/orphan is recorded.
Earlier harness failures and cropped framing remain visible in the seal, with
corrections explained in the selected README. No native assertion was weakened.
All Blender/Godot processes used the unchanged shared `run.ps1`, private per-job
APPDATA/local/temp/Blender resources, the shared GPU mutex and existing-process
checks. Restores reuse their capture's private `users/candidate-capture-<renderer>/ART07G1`
checkpoint; no owner's live save path was used. No automatic approval rejection occurred.

## Candidate, commands and limits

Fresh seal: `D:/project-wroughtwild-art07-r5/build/art07-repairs/r5/v02/handoff`

Manifest SHA-256: `18a9c2bf4a659373ccd303f8682e3f7553146a25619e736d66c969fb85f7f986`

Exact payload: 5,444 files / 3,931,408,290 bytes, excluding the
separately hashed root `manifest.json`. The receipt is outside its own seal.
Standalone full file-set/hash verification passed. Applying just the nine declared
deltas to fresh `v03/runtime` also passed: all 4,787 original runtime entries match
the engine-tested v02 bytes. v03 was not engine-run; the fresh import and process
restore evidence belongs to v02.

Run from `D:/project-wroughtwild-art07-r5`:

```powershell
$repairPython = 'C:/Users/Matty/.cache/codex-runtimes/codex-primary-runtime/dependencies/python/python.exe'
$seal = 'D:/project-wroughtwild-art07-r5/build/art07-repairs/r5/v02/handoff'
& $repairPython tools/wroughtwild-art07-repairs/r5/package.py verify $seal
```

The completed source/runtime commands and private paths are retained exactly in
`evidence/selected/results.json`, original job specs, and each log's `.log.json`.
The final guarded batch was:

```powershell
& tools/wroughtwild-art07-repairs/run.ps1 -Spec build/art07-repairs/r5/v02/complete-candidate.json
```

It contains corrected packed reopen, matched baseline captures, the generated
candidate import/smokes, and candidate capture/restore/benchmark/terrain jobs in
both renderers. Existing output versions must not be overwritten. For independent
review, choose an absent version (v04 is absent at handoff):

```powershell
& $repairPython tools/wroughtwild-art07-repairs/workspace.py prepare --id r5 --version v04
& $repairPython tools/wroughtwild-art07-repairs/r5/apply_package.py --package $seal --runtime D:/project-wroughtwild-art07-r5/build/art07-repairs/r5/v04/runtime
& $repairPython tools/wroughtwild-art07-repairs/r5/prepare.py install-harness --version v04
& $repairPython tools/wroughtwild-art07-repairs/r5/evidence_jobs.py v04 review
& tools/wroughtwild-art07-repairs/run.ps1 -Spec build/art07-repairs/r5/v04/import-smoke.json
& tools/wroughtwild-art07-repairs/run.ps1 -Spec build/art07-repairs/r5/v04/review-evidence.json
```

Defer if another GPU job is active; never stop it. Do not play or import inside
the seal. `audit.py` and `curate.py` document the selected v01/v02 comparisons;
`source_jobs.py vNN` generates new source jobs only in an absent job set.

Remaining limits: slab-concealed feet; cosmetic void/native solid-body mismatch;
unchanged wall/low-ceiling lid overlaps; three flat terrain probes rather than
arbitrary slope coverage; capture-paced samples rather than an uninterrupted human
play session; unassigned target-hardware budgets; R2 overlap reconciliation; pending
owner visual acceptance and independent R8/R9 review. No missing native body/seat
or geography decision was needed for this bounded visual candidate. None of those
limits is silently presented as fixed.

## Commits and publication

Checked implementation: `f4d5ef753e22fe6ef69164db25f768a2a289f248`.
Receipt commit is resolved from Git history after this receipt is written.
Local R5 commits only. No main integration, push, shared delivery update, other-task
messages, additional task launch or monitor was performed.
