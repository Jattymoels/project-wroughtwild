# ART-07R2 - Reduce setup time and asset residency

Status: ready_for_integration. Fresh v04 import/reopen: passed.
Owner visual acceptance: pending. Ordinary-world rollout: outside scope.

## Source and scope

Worktree `D:/project-wroughtwild-art07-r2`, branch `codex/art07-r2`. The actual
clean starting HEAD and inspected published `origin/main` were
`473ae7604b3841899c29629cf59f8836ed528424`. The runtime remains pinned to
`6bb2e044dcd0bf1788896aa2c19cdf56fee93522`. Wave 1 has no predecessors or direct
candidate dependencies. No peer candidate or newer main game/data was consumed.
The owner checkout, peer worktrees, normal saves and running playtests were left
intact. Main integration and push are reserved for the serial publisher.

Consumed common G1 source:
`D:/project-wroughtwild-art07-g2/build/art07/g2/v01/sealed`, manifest SHA-256
`fd5c592ef52185cc7d0737840e41af09dbb5bcea36b719539931e156f42865cd`
(5,701 files; 7,430,015,212 bytes). Sealed G2 review:
`D:/project-wroughtwild-art07-g2/build/art07/g2/v02/handoff`, manifest SHA-256
`49827ac3fe3522a92cb74eecdf60cd857e1157b08f5d57f2d273c617606e93a1`
(772 files; 594,208,921 bytes). Full `workspace.py verify --id r2` input results
and a repeated pre-application verification are retained in the package. The
4,787 original runtime entries were rehashed before launch and after all tests.

The production delta contains 238 modified original files and two added files:
B3 call-local terrain-query memoization; B1 numeric radius reuse; nine existing
texture consumers using an exact identity resolver; complete C1/C6 shader-input
cache keys; and 226 lossless interchange image-reference derivatives. The resolver
and 723-entry alias map are the two added production files. Fifty-three additive
evidence files remain isolated under `game/r2`.

`changes.json` in the package lists every actual file, function/setting, before/after
hash, purpose and overlap. R8 must compose R1's final fit, R3/R7's final materials
and image references, and the noted R5 chest/R6 supported-surface edits. Regenerate
aliases from final PNG bytes and complete import parameters. Do not overwrite a
peer runtime with whole R2 files. R4 lifecycle diagnostics remain visible.
See [application instructions](../../../../art/leyline-studies/2026-09-14/art07-repairs/r2/APPLICATION.md).

The only Git-owned paths are `tools/wroughtwild-art07-repairs/r2/`,
`docs/art/leyline-studies/2026-09-14/art07-repairs/r2/`, and these two `receipts/r2`
files. Shared setup/delivery/queue files were not edited.

## Result and checks

| Current RTX 5090 result | Forward+ | Compatibility |
| --- | --- | --- |
| Original G1 median setup seconds | 91.559 | 87.130 |
| R2 median setup seconds | 58.742 | 56.785 |
| Median reduction | 35.84% | 34.83% |
| Loaded textures, G1 to R2 MiB | 1,307.77 to 867.07 | 1,288.49 to 789.82 |
| Primary buffer range before and after MiB | 206.11 to 212.84 | 203.89 to 210.61 |

There are 24 fresh already-imported benchmark processes: three art-on and three
art-off per backend both before and after. Every R2 art-on setup is below every
original G1 observation in its backend. All use the same paid checkpoint,
cameras/resources, 1440 x 900, 4x MSAA, vsync off, max_fps 0, 120 warmup and 300
settled samples per camera/light. Actual Godot is 4.5 stable official
`876b290332ec6f2e6d173d08162a02aa7e6ca46d`, with AMD Ryzen 9 9950X3D, Windows
10.0.26200, Vulkan 1.4.325 and OpenGL 3.3.0 NVIDIA 591.86.

Every individual setup, min/median/max, loaded allocation, first-focus/warmup stall
and settled cost is in `documentation/costs.md` and its JSON. Import observations
(85.300 seconds original, 70.828 diagnostic, 63.085 candidate) are separate single
runs. The original-method diagnostic identifies B3 projection (68.630 inclusive
seconds) and B1 visual fit (12.740) as major repeated work. Nested spans are not
summed. Controller traversal is a separate uncaptured measurement: all six runs
complete the same 135.078021510504-metre route. Single-run R2 worst frames remain
437.400 and 456.368 ms; these are not settled costs or repeatability bands.

The pre-seal engine index has 110 successful fresh processes and seven retained
failed attempts. It records exact commands, private paths, times, exit statuses,
combined-log hashes and diagnostics at `evidence/v03/analysis/engine-jobs.json`.
The 60 candidate native jobs in `documentation/CHECKS.md` include 398 unit checks,
all 273 legal pairs (1,861 catalogue checks per backend), original source work and
partial/final restore checks, 16,676 projection/differential checks, F4
pause/block/cancel and partial/exhausted restore, paid acquisition/restart, and
Living Frontier continuation/restores. Source replay inverses retain every original
assertion. The fresh final-hook art-on/art-off fingerprints exactly match each
other and the explicitly historical pinned G1 fingerprints across all six owner
fields and geography hash
`550b2d742314136a71a8c177920059bd245996e744b8722c8ba8c68afbde4a70`.

Three frame-paced home/route/read cycles plus native partial work, release/re-entry,
finite depletion and fresh restart pass in both renderers (43 flow and 14 restart
checks each). The unchanged G1 frame-paced reference also passes. Texture allocation
is flat across cycles 1 to 3. Forward+ post-reload buffers are 303,696 bytes above
G1, and both add 6,144 bytes per later cycle; Compatibility buffers match. Three
cycles do not establish leak freedom. Immediate same-frame retirement produces the
same material-null error in fresh unchanged G1 and R2; all failed logs remain.
`documentation/DIAGNOSTICS.md` explains the corrected additive harness defects,
serialization precision and unchanged baseline lifecycle limit. Fatal errors were
not suppressed or relabelled as successful runs.

Sixty actual image pairs at 1440 x 900 match exact resource state, ordered visible
and hidden part geometry/poses, and retained scene/cache geometry. Fifty-nine pairs
are byte-identical; the remaining pair differs by one RGB channel level at one
pixel, with identical alpha. Unique retained base geometry is unchanged across the
wider views: 1,034 to 1,679 meshes and 1,126,091 to 2,674,818 triangles, including
hidden stages and explicit LOD caches. Alternate importer index-LOD triangles are
not added to base triangles; renderer buffers are separately recorded.

Actual motion retains 60 native F4 work/pause/block PNG frames and 77 native
controller-route frames, with traceable small GIF previews. The paid-world matched
images and paused native view were shown in chat. These are actual engine outputs;
no image generation or retouching was used. No human first-hour or real-time motion
benchmark is claimed for scripted/capture-paced inspections.

All engine jobs use the unchanged shared runner, single
`Local\Wroughtwild-Art07-GPU` mutex and existing-process checks. Benchmarks are
separate from imports, generation and captures. Each job has private APPDATA,
LOCALAPPDATA, TEMP/TMP and Blender resources; `ART07G1` resolves under that private
APPDATA. No other process was stopped. The extra v04 import/reopen was deferred
while R1 owned the GPU slot, then completed through the same mutex after release.
There were no automatic approval rejections.

Production tuning values introduced: none. Existing fit constants, projection
reach, texture sizes/mipmaps/colour spaces, stages, LODs, density and streaming
ranges are unchanged. Evidence controls are explicit: fixed 60-frame inspection
cadence, two process frames plus a rendered boundary between reload visits,
1/30-second native F4 motion ticks and the original 45-frame traversal settle.
Their purpose is reproducible measurement/inspection, not gameplay tuning. The new
cost-summary derivative uses ASCII ` to `; the sealed G2 report remains unchanged.

## Candidate and limits

Package:
`D:/project-wroughtwild-art07-r2/build/art07-repairs/r2/v03/handoff`

Manifest SHA-256:
`f5b3ea9559e9f4b76059c7863730b438bdfbe7a6f8f689ae3bae75340cb01ac0`

Exact payload: 6,411 entries / 9,091,108,107 bytes; the separately hashed
`manifest.json` is excluded from those payload totals. `disk-costs.json` separates
3,438,342,971 production runtime bytes (4,789 files) from evidence/master overhead.
All 1,061,048,169 redundant PNG disk bytes remain. The production runtime grows by
85,513 bytes; the saving is measured resident textures, not disk bytes.

The original 31 packed master files and source lineage/recipes remain under
`g1_originals/`. Original changed-file bytes are under `baseline_sources/`;
modified interchange source scenes/scripts are under `game/`. Non-image glTF JSON,
GLB non-JSON chunks, geometry, UVs and original maps are preserved exactly. No mesh
was silently rebuilt from a newer game/data revision. The normal game/sim/data,
saves, all legal shapes, native collision/targeting, finite stock, paid ownership,
work states and saved geography are preserved. No body, seat, geography or native
architecture rule was invented. Original import settings and LOD selections remain.

The seal excludes its own receipt. Its exact delta has been applied to freshly
verified `D:/project-wroughtwild-art07-r2/build/art07-repairs/r2/v04/runtime`.
Fresh actual import and both renderer reopens passed (exit 0): import 60.077
seconds, Forward+ 61.861 seconds, Compatibility 60.195 seconds, all whole-job wall
times. All 4842 declared source files still match exact hashes after import.
The separate [post-seal record](../../../../art/leyline-studies/2026-09-14/art07-repairs/r2/post-seal-verification.json)
contains actual commands/log hashes, source audit, and commit content proof. Its
SHA-256 is `8a7c981337527ef0b9f5729b90022e07261f43f7e01c8cb79a6efad1dbc41743`. This adds evidence outside the unchanged seal.

Run from this assigned worktree with the installed Python:

```powershell
$repairPython='C:/Users/Matty/.cache/codex-runtimes/codex-primary-runtime/dependencies/python/python.exe'
$repairPackage='D:/project-wroughtwild-art07-r2/build/art07-repairs/r2/v03/handoff'
$repairHash='f5b3ea9559e9f4b76059c7863730b438bdfbe7a6f8f689ae3bae75340cb01ac0'
& $repairPython -B tools/wroughtwild-art07-repairs/r2/handoff.py verify --package $repairPackage --manifest-sha256 $repairHash
```

For another reconstruction, use an absent version; `v05` was absent at receipt
creation. Inspect/verify gates first. The apply command refuses conflicting sources:

```powershell
& $repairPython -B tools/wroughtwild-art07-repairs/workspace.py inspect --id r2
& $repairPython -B tools/wroughtwild-art07-repairs/workspace.py verify --id r2
& $repairPython -B tools/wroughtwild-art07-repairs/workspace.py prepare --id r2 --version v05
& $repairPython -B tools/wroughtwild-art07-repairs/r2/handoff.py apply --package $repairPackage --manifest-sha256 $repairHash --target 'D:/project-wroughtwild-art07-r2/build/art07-repairs/r2/v05/runtime' --with-evidence-harness
& tools/wroughtwild-art07-repairs/r2/queue.ps1 -Specs @('build/art07-repairs/r2/v05/import-smoke.json')
```

Target-device acceptance remains unverified until the owner selects a device and
it is actually tested. Owner visual review and combined R8 verification remain
open. Current RTX 5090 results are not a minimum-hardware budget. No ordinary-world
rollout is authorized by this isolated receipt.

## Commits and publication

Checked implementation commit:
`ca370f52a20637548da0cf1fd04cdc9dbeb9c440`.

The commit's 35 files match the sealed tools/documents in content: 20 byte-identical,
15 differing only by Git's CRLF-to-LF normalization, with both hashes retained in
the post-seal proof. Runtime production files have separate exact hash checks.
The receipt commit also normalizes the Git README and receipt heading to ASCII;
the immutable seal retains its original bytes. Commands, costs, production source
and engine evidence are unchanged. The receipt commit is resolved from Git history
after this file is written:

```powershell
git log -1 --format=%H -- docs/prototype/art07-repairs/2026-09-14/receipts/r2.json
```

Main integration and remote push: not performed by worker. The serial publisher
verifies the package, composes the owned changes, integrates and pushes main under
the standing permission. No shared delivery row was changed.
