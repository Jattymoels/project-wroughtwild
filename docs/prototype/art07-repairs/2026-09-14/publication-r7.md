# ART-07R7 publication — 14 September 2026

R7 composes leafier shrubs, layered fern/grass groups and rooted sway at existing decorative anchors. It keeps their original envelopes, IDs, poses, clearing masks and visibility distances, while retaining the published R1 canopy and native world/save state.

The complete seal and all original/dependency inputs were rehashed. The original 48,025-check evidence collector was reproduced with copies and writes redirected. Fresh reconstruction applies the published 105-file R1 parent delta once, then only R7 own changes. A new headless import, all-vertex wind/envelope scan, native grounding bootstrap and building/excavation fixture pass, along with before/after paid construction and separate-process restores. Initial snapshots match exactly; paid/final snapshots and actual save files differ only in the generated scene names of two stations. Stable station keys/types, ownership, poses, stock, geography and every other field match exactly. Both fresh-process restores pass all eight original assertions. Raw saves and exact field differences are retained; fresh byte identity is not claimed. The two original packed B2 masters reopen in background Blender. The worker retains both-renderer views, catalogues and separate benchmarks.

Publisher inspected the actual matched near/middle/far Forward+ sheet and Compatibility habitat view. Fern groups are more layered and shrubs have finer leaves, but some shrub silhouettes are smaller; dark/colder shade and extensive bare middle ground remain.

The [machine-readable publication](publication-r7.json) retains exact
commit mapping, package identity, worker logs, reproduced verifier results and
fresh process receipts. The [worker receipt](receipts/r7.md) retains full
source/tuning details, costs, preserved failures and limits.

## Fresh publisher checks

Runtime: `D:\project-wroughtwild-art07-r7\build\art07-repairs\r7\v03\runtime`. All 4,918 expected source
entries still match after execution. Runs use fresh private state and logs with
the unchanged shared runner and serial GPU mutex.

The declared runtime has 4,919 entries. The remaining entry is the generated `game/r7/replayed-paid-home.json.previous` checkpoint, rotated by the unchanged SaveManager during the fresh test. Its only semantic differences are the same two generated station scene names; all code, assets and authored inputs remain byte-identical. Raw before/after hashes and field differences are recorded in the publication JSON.

Fresh Godot jobs are headless except the native grounding bootstrap, which uses Compatibility with a publisher-only startup no-focus override and verifies that the mouse is visible; that fixture instantiates no Player. Its original 16 assertions are unchanged. The initial headless attempt failed three root checks: an independent transform probe confirms that the pinned dummy renderer returns identity MultiMesh transforms, while the real renderer returns the exact assigned transform and passes all 16. Both logs and the diagnostic are retained. The unchanged ecology fixture matches the worker headless 102 checks. Blender is explicitly background. Worker images and benchmarks were independently inspected and hashed; cross-renderer appearance and benchmark timings were not freshly repeated.

| Job | Wall seconds | Exit |
| --- | ---: | ---: |
| publisher-import | 83.69 | 0 |
| publisher-mesh_checks | 1.27 | 0 |
| publisher-dummy-diagnostic | 0.28 | 0 |
| publisher-grounding-rendered | 3.10 | 0 |
| publisher-ecology | 192.01 | 0 |
| publisher-paid-before | 127.75 | 0 |
| publisher-paid-restart-before | 82.44 | 0 |
| publisher-paid-after | 126.70 | 0 |
| publisher-paid-restart-after | 85.69 | 0 |
| publisher-reopen | 1.75 | 0 |

## Integration and remaining gates

Implementation `f293e038d8c6c8cd54a4f84f57a724ae491bb06c` became
`27f8a1f7f6133347a66fedd100ae1d03a8fc3951`; receipt
`2b0af7889b48da90bdfa881cf27942ae9a7e1474` became `eb9029dfe4dac3600546430f1f7e0081e79306df`.
The scoped cherry-pick preserves every owned Git blob. These notes accompany an
ordinary push to approved origin/main; remote-tip proof is verified after pushing.

G2-V05: partial; bounded local composition is technically verified. Continuous reference understorey is unmet, with owner and combined R8/R9 review pending.

Eligible retained envelopes cover at most 6.12–8.68 percent of the measured 32 by 32 m windows. Larger envelopes/new geography remain proposals. LOD2 stays selected and small cover disappears at inherited ranges. Direct standalone grounding preload failures remain recorded; the original 16 assertions run through the normal resource-graph bootstrap. R8 must apply R2 shrub image repackaging/aliases once and verify R7 material bindings, then remeasure combined costs. No source texture pixels, anchor generation, save migration or ordinary-world rollout changed.

R1–R7 are checked published candidates. R8 dependency gates are clear; its actual workspace setup is recorded separately once prepared. R9 waits for published R8 and must be an independent task.

All 273 legal building pairs, including octagonal/chamfer/triangle uses, native
bodies, finite stock, paid ownership and saved geography remain constraints.
See [deliveries](deliveries.json), [setup](SETUP.md) and
[publication checks](publication-r7-checks.json).
