# ART-07 repair contract

## Owner correction — prototype review limits, 14 September 2026

The owner stopped R9's multi-hour review. Follow the current owner depot's
`C:/Users/Matty/Dev/project-wroughtwild/AGENTS.md` Prototype work limits first:
10 minutes total for review/verification, up to three focused jobs, one renderer,
reuse of applicable existing evidence, and explicit approval before exceeding it.
The exhaustive requirements retained below describe the original pack and are
superseded where they conflict with that later instruction. Do not restart old
workers or recreate their evidence to comply with historical wording.
R9 is closed out from available results; no further testing or packaging is
authorised by its old prompt. A review can finish with explicit unverified limits
and references to the existing R8 package instead of sealing another copy.

The owner requested the plan, prompts and setup on 14 September 2026. This pack
prepares follow-up work on the seven G2 findings. Sending one worker prompt selects
that bounded task; preparing its directory does not implement it. The original
26 ART-07 deliveries remain complete and historically unchanged.

Read AGENTS.md in its required order, then this contract, SETUP.md, inputs.json,
the assigned plan row and its linked reading. Accepted D-013/D-030 art direction,
native D-017 construction and the existing world/save contracts remain authoritative.
Restate the outcome, affected presentation/source files, assumptions and small plan.
Stop only the dependent change if a missing decision changes body/seat authority,
geography, player rules, save compatibility or architecture; provide measured options.

## Baseline and ownership

- Git review baseline: `b8248561bb06268af607d718c119c69639e4dadc`.
- Game/data/native baseline: `6bb2e044dcd0bf1788896aa2c19cdf56fee93522`.
  A newer documentation checkout does not authorize a new runtime/native revision.
- inputs.json records the checked G1 runtime, complete unmodified G2 source copy,
  G2 review evidence, native DLL and original 24-source index. Check real paths and
  hashes before consuming them. A Git clone has no ignored asset packages.
- Each R task owns only its new tools/evidence directories and two receipt files
  listed in plan.json. Originals under tools/wroughtwild-art07, all earlier seals,
  other worktrees, normal game/sim/data, saves and shared dispatch files are read-only.
- Work in `D:/project-wroughtwild-art07-rN` on `codex/art07-rN`. Generated files
  live under that worktree's `build/art07-repairs/rN/<fresh-version>/`. Never
  overwrite an earlier candidate. Do not remove worktrees, reset, clean or stash.
- Exclusive Git paths prevent source conflicts; copied runtime paths can still
  overlap. Every handoff must identify the actual file/function/settings changes.
  R8 resolves overlaps against the common baseline, never by last-file-wins copying.

## Immutable gameplay and presentation limits

Preserve all 273 legal shape/material combinations and octagonal/chamfer/triangle
uses, lattice resolution, roof/material unlocks, costs, capacities, finite stock,
native work/escrow/drive/heat, failed-placement kit retention and one-object success.
Preserve world profiles, seeds, resource/anchor IDs and poses, terrain/excavation,
collision, targeting and paid checkpoint state. Keep F4 fractional/exhausted
appearance restored synchronously before recovery input. No new scatter, hidden
stock, recipe, body, rig, era, mob, renderer framework or ordinary-world adoption.

R1 owns trunk/crown geometry and fit; R2 owns measured loading/resource reuse;
R3 owns building face/end/edge treatment; R4 owns attributed cleanup lifecycles;
R5 owns chest visual fit; R6 owns retained ore-surface presentation; R7 owns
retained-anchor composition. Source-owner names provide context, not permission to
edit their live worktrees. R8 integrates all seven; R9 independently reviews them.

## Execution and evidence

Use the installed local tools and selected source assets. Read
[the original production process](../../art07-production/PROCESS.md) when source
art needs changes. Reuse packed masters; direct-model exact construction in Blender.
Use the imagegen skill only when actual new raster inputs are needed. No new paid
service, model download or third-party dependency is authorized by this setup.

Keep the canonical G1/G2 wrapper guards unchanged. The setup runner is a small
derivative of the checked G2 runner with repair-output guards and a noninteractive
stop of only its own failed child. All imports, rendering and benchmarks use
`Local\Wroughtwild-Art07-GPU`, read-only process checks and hidden owned processes.
Defer if an existing game/art/compiler process is active; never interrupt it.
Use private APPDATA, LOCALAPPDATA, TEMP/TMP and Blender resource directories inside
the task build tree. The retained `ART07G1` custom user-dir name must resolve inside
that private APPDATA. Do not launch copied Godot directly with the owner's save paths.

CPU/source work can proceed in separate tasks; GPU jobs are serial. Benchmarks
run alone, separated from generation, imports and captures. Record commands,
processes, hardware, settings, exact sample counts and log hashes. Keep failures
and expected diagnostic warnings visible. Never relax assertions or suppress logs.
The current RTX 5090 is the initial measurement machine; minimum target hardware
and acceptance budget remain open unless the owner supplies them. Record the actual
device in each result, even if it differs from this planning record.

Repeat only meaningful checks for the changed behavior. Changed models/maps require
fresh packed-master reopen and a fresh runtime import. Stateful changes need real
native work plus a separate-process restore. R8/R9 run the combined gates. Use actual
controller movement and native ticks for motion; label harness-paced acquisition,
inspection stock and illustrative playback honestly. Show actual model renders and
motion in chat and preserve their provenance. Runtime passes do not imply owner
visual acceptance, continuous human first-hour completion or target-device clearance.

## Handoff and publication

Use [receipts/TEMPLATE.md](receipts/TEMPLATE.md) and its JSON schema example.
Deliver a new sealed candidate with exact file-set/hash verification, source/master
lineage, dimensions and controls, and a reconstruction/application recipe. Its
`changes.json` lists each baseline-relative file, before/after hash, affected function
or setting, purpose, replacement asset and any overlap with another R task. Retain
original source bytes and distinguish text line endings from semantic edits.
For dependent candidates, list the exact applied parent packages separately and
record the before-hash against that named parent state. Include only your own delta
in changes.json; do not present consumed parent changes as newly owned repairs.
R8 accounts for those parents once when reconciling overlapping deltas.
Document every new presentation tuning value and the experience it controls.

Workers commit only their own tools/evidence/receipts on their R branch and report
the exact commit(s), package path/hash, checks and remaining limits once, then stop.
A receipt can name an earlier source commit; the publisher resolves the later
receipt commit from actual Git history. Workers neither push main nor edit the
shared delivery index. The publisher serially verifies, integrates and pushes under
standing permission, then updates deliveries.json and readiness. A blocked finding
does not satisfy a dependency or count as a completed repair.

Do not send other tasks messages without explicit owner authorization. Do not
restart completed workers for informational notices, send acknowledgement requests,
create recurring checks or start the next task. Ordinary-world rollout and the
six ART-06C animal rig/adoption items remain outside this entire repair pack.
