# ART-07R6 publication — 14 September 2026

R6 adds ore-specific mineral, cracking and depletion detail to all five native faceted ore surfaces. It preserves the original thin terrain ribbon, picking triangles, finite yield, excavation and saved state; the raised authored fallback remains unchanged.

The complete seal and all original/dependency inputs were rehashed. The original preservation verifier was reproduced with output writes redirected, and the source-field atlas was independently regenerated byte-for-byte. A fresh reconstructed runtime passes headless surface/work/excavation/controller checks and a separate partial-restore process, followed by background Blender reopen of the changed packed source. Fresh geometry, inventory and geography reports match the rendered worker records. The worker supplies 21 selected passing jobs across both renderers; their logs and image provenance were verified.

Publisher inspected actual Forward+ full iron at player height and Compatibility partially worked iron at close range. The mineral treatment follows the original narrow surface and reads as a flat strip; it does not supply the raised source silhouette.

The [machine-readable publication](publication-r6.json) retains exact
commit mapping, package identity, worker logs, reproduced verifier results and
fresh process receipts. The [worker receipt](receipts/r6.md) retains full
source/tuning details, costs, preserved failures and limits.

## Fresh publisher checks

Runtime: `D:\project-wroughtwild-art07-r6\build\art07-repairs\r6\v02\runtime`. All 4,798 expected source
entries still match after execution. Runs use fresh private state and logs with
the unchanged shared runner and serial GPU mutex.

A publisher-only child fixture omits 80 flow and 20 restart image-write assertions (five states at two distances for five ores, plus 30 controller frames in the flow). All native assertions, source/collision checks and actual controller traversal remain; only capture stalls and image output are omitted. The sealed review and runtime code are unchanged. Fresh shader appearance and benchmarks are not claimed from headless execution.

| Job | Wall seconds | Exit |
| --- | ---: | ---: |
| publisher-import | 74.25 | 0 |
| publisher-flow | 26.13 | 0 |
| publisher-restart | 17.48 | 0 |
| publisher-reopen | 17.56 | 0 |

## Integration and remaining gates

Implementation `78ef855ca9cb6ca1f4d16221cca98d53a9d1aa8a` became
`b483d2027b6c3326dd25483a3f9f2d4f7bcd8c12`; receipt
`f44920e696b5c81b7fa6548e4f86a3c9641fec96` became `5619672bbaf56cc3d7b0f2955ff2a81fdfc54c4a`.
The scoped cherry-pick preserves every owned Git blob. These notes accompany an
ordinary push to approved origin/main; remote-tip proof is verified after pushing.

G2-V04: partial; technically verified surface-material candidate, with owner visual acceptance and combined R8/R9 review pending.

Raised ore geometry and physically recessed altered scars require a separate terrain/body decision. Five selected sites are not exhaustive support coverage. Worker current-machine RTX 5090 benchmarks are not target-hardware clearance. R8 must merge the C5 material/state hooks with R2 reuse and combine the independent G1Art.resource mappings from R1/R7. No ordinary-world rollout is included.

R8 still waits for checked R7 publication.

All 273 legal building pairs, including octagonal/chamfer/triangle uses, native
bodies, finite stock, paid ownership and saved geography remain constraints.
See [deliveries](deliveries.json), [setup](SETUP.md) and
[publication checks](publication-r6-checks.json).
