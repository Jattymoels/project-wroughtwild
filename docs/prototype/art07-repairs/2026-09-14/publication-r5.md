# ART-07R5 publication — 14 September 2026

R5 reshapes the lower cabinets and adds four inset feet across all eight chest families. Cabinets meet ordinary slabs and clear fine slabs while native bodies, paid placement, 960-unit storage, saved ownership and the original lid/hinge authority remain unchanged.

The publisher rehashed the complete 5,444-file seal and all required original/dependency packages, reproduced every worker audit assertion with only its output paths redirected, and checked selected image/motion provenance and all retained runner-log hashes. Fresh v04 starts from 4,787 verified original runtime entries, applies exactly nine sealed deltas and adds four fixture files. A new headless import, 250 native checks, 32 separate-process restore checks, and background packed-master reopen/render plus 16 GLB imports pass. The fresh 12-case geometry/native records and full ownership/block checkpoint equal worker rendered evidence; packed-source measurements match exactly.

Publisher inspected actual before/after Forward+ ordinary-slab frames, Compatibility fine-slab contact, retained paid-home contact and packed-source open views. The cabinet looks shallower and feet ground the flat-terrain version. Existing room shading remains dark. Owner visual acceptance is pending.

The [machine-readable publication record](publication-r5.json) contains exact
commits, package identity, delta paths, worker logs and fresh process receipts.
The [worker receipt](receipts/r5.md) retains the complete fit controls, measured
clearances, native proof, warning history and current-machine costs.

## Fresh publisher checks

Runtime: `D:\project-wroughtwild-art07-r5\build\art07-repairs\r5\v04\runtime`. All 4,791 expected
runtime source and fixture entries still match after execution. Fresh state and
logs used the unchanged shared runner and single GPU mutex. Godot ran headless;
Blender ran in background mode. No desktop pointer automation or rendered Godot
window was used, and no game/input code was changed for verification. This proves
the chosen checks can avoid foreground mouse capture; it does not fix every launcher.

| Publisher job | Wall seconds | Exit |
| --- | ---: | ---: |
| import | 78.28 | 0 |
| publisher-headless-checks | 19.03 | 0 |
| publisher-headless-restore | 3.03 | 0 |
| publisher-blender-reopen | 112.46 | 0 |

## Integration and remaining gates

Original implementation `f4d5ef753e22fe6ef69164db25f768a2a289f248` becomes `7f658e1c343ff281002e6951258e800667aad18b`.
Original receipt `24e14c82437bfca62576a14a328f3fb445fb6f2d` becomes `c057b9210f60417ef81884a0c2f799bbede37d3d`. A scoped two-commit
cherry-pick preserves every owned Git blob because main had advanced separately
with desktop-comfort guidance. Publication notes accompany an ordinary push to
approved origin/main; remote-tip verification is reported after that push.

G2-V03 remains partial: feet are concealed 0.125 m in ordinary slabs and 0.0625 m in fine slabs. The cosmetic under-cabinet void retains the solid native collider. Existing wall and low-ceiling lid overlaps remain; the three terrain probes cover flat retained sites, not arbitrary slopes. Source triangles rise from 6,336 to 6,768 per closed chest (+432, 6.82%). Both-renderer visual/terrain/benchmark evidence is retained and verified from the worker; the publisher did not repeat rendered Godot or performance jobs. Current-machine timings are not target-hardware acceptance. R8 must reconcile eight replacement GLBs with R2 texture storage and R9 must independently review the combined candidate. No ordinary-world rollout is included.

All 273 legal building combinations, including octagonal/chamfer/triangle uses,
remain intact through unchanged game/data/native bytes outside the visual delta.
The fresh replay exercises the affected chest cases, not all unrelated journeys.
No new player tuning or native/save design decision is introduced.

R1–R5 are published candidates; R8 waits for checked R6 and R7 publication.
See [deliveries](deliveries.json), [setup](SETUP.md) and
[publication checks](publication-r5-checks.json). The owner requested a short
accomplishment summary with every completed slice; that reporting preference is
also recorded in the repository instructions.
