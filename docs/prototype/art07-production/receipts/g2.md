# ART-07G2 — Independent full-kit review

Status: technically delivered — bounded technical clearance of the sealed G1
review package, with visual, cost and fixture-cleanup handbacks.
Owner visual acceptance: pending. Ordinary-world rollout is not approved by G2.
Completed: 13 September 2026. This session did not implement G1; G2 only.

## Scope and lineage

The assigned existing IDs are `lf_white_stag`, `lf_green_moth`, `lf_paired_boar`,
`lf_red_boar`, `lf_blue_boar`, `ember_whelp`, `cinder_archer`, `ash_hound`,
`stone_husk`, `shrieker`, `gloom_crawler`, `bog_lurker`, `marsh_wisp`,
`cinder_wisp`, `hollow_knight` and `valley_elk`. All 16 native definitions,
visual redirects, original GLB hashes, body/combat definitions and presentation
scripts remain the pinned runtime. No new mob mesh, rig, gameplay rule, tuning,
normal-world adoption or next slice is implemented.

Worktree: `D:/project-wroughtwild-art07-g2`, branch `codex/art07-g2`.
Published review base: `8439b5a7745ca491b694406789273a412aec44d2`.
Game/data/native pin: `6bb2e044dcd0bf1788896aa2c19cdf56fee93522`.
The original C: owner checkout stays on main with its pre-existing five modified
LF6 captures and unrelated local fixtures/imports preserved. D: was selected
because C: had about 1.8 GiB free; nothing was deleted to make room.

Consumed G1 implementation: `1d3d3ce29c4afe7e1db970b880953e92035a0b7b`;
receipt: `4412781c70ce8cbc7651f9262df6831593b83874`. Published ancestry and the
actual remote main tip were checked against
`https://github.com/Jattymoels/project-wroughtwild.git`.

Canonical G1 package:
`C:/Users/Matty/Dev/project-wroughtwild/build/art07/g1/worktree/build/art07/g1/v06/handoff`.
Manifest SHA-256:
`fd5c592ef52185cc7d0737840e41af09dbb5bcea36b719539931e156f42865cd`.
All 5,701 entries / 7,430,015,212 bytes verify before and after review. The checked
G2 input record and original G1 index resolve all 24 published prerequisites
(B1-B4, C1-C6, D1-D6, E1-E3, F1-F5): 35,779 files / 19,093,094,616 bytes, all
independently rehashed twice. Exact absolute paths, original input/raw source
hashes, selected versions and receipt commits are in `audit/inputs.json` and
`audit/after-inputs.json` of the G2 package. The original index SHA-256 is
`eb39916df348f51dae7643692becaa5340ff9bfede2ed38f5b0ecdfb21ee5fef`.

Native DLL SHA-256:
`0e7c4956b6c870f5879e78f54c5830509b7ae37913055543cc3a6d451298e0e1`.
godot-cpp archive SHA-256:
`701934d2505314d4272ef258beca75974de9f2bb2d94d98b29d22670de42370c`.
No native rebuild, third-party dependency, image generation or TRELLIS run was
needed. Existing Blender 4.5.9, Godot 4.5 stable `876b29033`, bundled Python/Pillow
and the verified explicit-path G1 reopen/verifier recipes were reused.

## Implementation and review

G2 adds only isolated copy/run/audit helpers, inspection scenes, evidence and
this receipt. A complete byte-verified G1 copy lives at
`D:/project-wroughtwild-art07-g2/build/art07/g2/v01/sealed`.
The separate mutable runtime is
`D:/project-wroughtwild-art07-g2/build/art07/g2/v01/review`:
4,787 initial files / 3,438,257,458 bytes, imported without inherited caches.
G1 copy/reconstruction/GPU guards were never edited or redirected.

The independent archive audit compares pinned Git blobs, records exact CRLF
export differences, and reconstructs all 16 original runtime presentation-hook/
project-setting changes. The documented three G1 trailing-blank recipe
differences and 31 main-checkout line-ending differences remain distinct from
sealed hashes. No simulation, tuning or retained actor change is inferred from
line-ending equivalence. Full detail is in `audit/source-audit.json`.

The [review report](../../../art/leyline-studies/2026-09-09/art07/g2/README.md)
separates these findings:

- Technical: all 64 fresh engine/Blender subprocesses exit 0; no fatal script or
  engine error is accepted. Eight isolated source fixtures emit ObjectDB exit
  cleanup warnings (B3/C1/C2/C3/C4/E1/F2/F3); G2-T01 leaves attribution and
  long-session behavior open with their owners.
- Visual: G2-V01 narrow crowns (B1/C4/G1), V02 generic material joins
  (G1/D4/D5/D6), V03 inherited 0.15 m chest/floor intersection (E3), V04 C5
  fallback-only raised ore, and V05 unmatched B2/B4 support-role composition.
  These are bounded handbacks, not repairs or new placement/geography rules.
- Cost: G2-C01 leaves texture/hidden-stage residency, setup delay and selected
  target-device testing open with G1 and source owners. No invented budget or
  owner acceptance is inferred from RTX 5090 frame rates.

The live tree audit measures 289 wood instances at horizontal scale 0.2694963
and 32 pine at 0.1313987, both vertical scale 1. Whole-tree compression includes
crowns while keeping the existing 0.7 m body. G2 changes no transform. The
selected fuller habitat is not achieved by these thin, separated crowns.

F4 retains its 1.5 x 1.45 x 1.45 m feeder body, 1.05 x 1.25 x 1.05 m pocket,
ground origins, four native yaws and centre + (0,0.85,0) attachment origins.
Its inherited drum pivot is (0.33,0.66,0.02), with native paid progress driving
rotation; pause/blocking hold escrow and pose. The 8 s clay-brick recipe,
finite 24-stroke source, capacity/contents and paid drive remain native.
All other source dimensions, pivots and sockets remain the sealed contracts.

Material albedo remains sRGB, normal/ORM linear; metric UV/triplanar mappings
and source hashes are unchanged. The F4/F5 static Blender view binds the
published external maps. Emission-off Godot and clay views show real recesses;
small F4 winding routes remain subtle and its frame/trays visibly regular.
Source incision/transform bounds remain inherited measurements, not a new G2
BVH-depth study. F4 emission controls do not disable the adjacent forge's heat.

All 30 original copied packed masters reopen with all 477 file images packed.
Six fresh GLB imports have finite vertices: workbench 1,832 triangles, forge
4,470, feeder 33,988, red source 11,999, green post 1,998 and roof hip 4,988.
Their staged Blender arrangement is a static inspection, not native placement
or another gameplay world. B1/C4 and cover use LOD2; F5 uses middle; hidden
work stages and other detail levels remain resident as documented in the costs.

Five LF hosts retain existing stag/moth/boar redirects. Six IDs still retain
older version-1 forms: cinder_archer, stone_husk, shrieker, gloom_crawler,
bog_lurker and hollow_knight. Their approved porcupine, bighorn ram, crane,
ground beetle, dragonfly nymph and tortoise ancestry remains unchanged but
unadopted. The two actual 16-ID lineups label selected and retained forms.
No new rig completion, ART-06C adoption or animal acceptance is claimed.

## Checks and evidence

The [check table](../../../art/leyline-studies/2026-09-09/art07/g2/checks.md)
and [hashed results](../../../art/leyline-studies/2026-09-09/art07/g2/checks.json)
record every process. Exact engine arguments, PIDs, durations, isolated paths,
stdout/stderr and log hashes are also in the local package's `logs/` and `audit/`.

Executed from `D:/project-wroughtwild-art07-g2` using the installed bundled
Python and G2 runner (v01 paths are occupied evidence; use fresh paths to repeat):

```powershell
$g2Python='C:/Users/Matty/.cache/codex-runtimes/codex-primary-runtime/dependencies/python/python.exe'
& $g2Python tools/wroughtwild-art07/g2/verify_copy.py build/art07/g2/v01
& $g2Python tools/wroughtwild-art07/g2/source_audit.py
& tools/wroughtwild-art07/g2/run.ps1 -Spec build/art07/g2/v01/core-jobs.json
& tools/wroughtwild-art07/g2/run.ps1 -Spec build/art07/g2/v01/restart-job.json
& tools/wroughtwild-art07/g2/run.ps1 -Spec build/art07/g2/v01/render-jobs.json
& tools/wroughtwild-art07/g2/run.ps1 -Spec build/art07/g2/v01/native-full-jobs.json
& $g2Python tools/wroughtwild-art07/g2/install_inspection.py
& tools/wroughtwild-art07/g2/run.ps1 -Spec build/art07/g2/v01/supplement-jobs.json
& tools/wroughtwild-art07/g2/run.ps1 -Spec build/art07/g2/v01/g2-benchmark-jobs.json
& tools/wroughtwild-art07/g2/run.ps1 -Spec build/art07/g2/v01/blender-jobs.json
& $g2Python tools/wroughtwild-art07/g2/verify_copy.py build/art07/g2/v01/after --verify-only
& $g2Python tools/wroughtwild-art07/g2/collect.py
& $g2Python tools/wroughtwild-art07/g2/preservation.py
& $g2Python tools/wroughtwild-art07/g2/package.py
& $g2Python tools/wroughtwild-art07/g2/package.py --verify
```

Independent art/baseline paid snapshots match initial, paid and final native
ownership and the actual 135.078021510504 m controller route. Canonicalization
omits only ephemeral station scene-node names, retaining persistent station keys.
Final-hook probes reproduce geography
`550b2d742314136a71a8c177920059bd245996e744b8722c8ba8c68afbde4a70`
and canonical snapshot
`2fe1ad3fa6b4581996b94b451d7d05c5d135c11281517f76f70bbf4ef1001aba`.
Both renderers independently pass 1,861 checks over all 273 legal pairs. D1-D3
exercise coarse/roof/fine octagonal/chamfer/triangle placement and reload uses.
Resource, station, work, kit-failure/one-success, inventory, save recovery and
partial-depletion cases replay unchanged assertions. F4 independently passes
227 work, 32 fractional-restore and 29 exhausted-restore checks per renderer,
including the delivered synchronous restore before recovery input.

Execution corrections are explicit: the initial `paid-art-restart` used
`--restore`, so it was a second full 865-check replay, not restart evidence.
`paid-fresh-restart` uses the actual `--restart` flag and passes eight checks.
The generator is corrected. The initial direct Git-byte check identified CRLF
exports; final comparison permits only the recorded conversion. A staged EOF
whitespace check trimmed four G2 recipes and the matching G2 fauna copy, changing
no executable statement. The preliminary v01 seal remains; the final fresh v02
seal includes `preservation-final.json`. No G1 assertion was weakened.

Current catalogue, session-plan and original-art validators pass; Python syntax,
22 local evidence links, 13 PNGs and all 198 motion frames validate.
Their exact command/results are in `audit/validation.json`.

Actual images and motion are embedded in the report. Home/catalogue: 1440x900;
F4: 1600x900; fauna: 1600x1200; Blender: 1440x1000, CPU Cycles 24 samples/eight
threads. Three animations contain 78 controller-route frames at an illustrative
300 ms cadence and 60 F4 native 30 Hz work/pause/blocked frames per renderer,
resized/encoded only. Source frame hashes are in `report/media.json`. The
harness-paced gathering/build targets are not an uncut human first-hour test.

On Windows 11, Ryzen 9 9950X3D, RTX 5090/591.86, 32,607 MiB VRAM, matched
1440x900/4x MSAA/vsync-off benchmarks use 120 warmup and 300 sampled frames per
camera/light, separate from captures and generation. All 16 matched pairs retain
camera/target/resource IDs/chunks. See the [full costs](../../../art/leyline-studies/2026-09-09/art07/g2/costs.md).

| Result | Forward+ art | Compatibility art |
| --- | ---: | ---: |
| Median frame range | 2.762-3.921 ms | 4.798-6.589 ms |
| p95 frame range | 3.134-5.023 ms | 5.544-7.407 ms |
| Worst sampled frame | 6.617 ms | 8.956 ms |
| Loaded textures | 1,307.8 MiB | 1,288.5 MiB |
| Setup through paid restore/camera | 91.306 s | 87.482 s |
| Same setup, baseline | 11.808 s | 11.319 s |

Art live unique triangles including hidden meshes span 2,420,221-2,503,669.
Setup is one already-imported process per mode/renderer, not a cold-disk or
repeated startup benchmark. The byte audit also finds 823 redundant standalone
PNG copies / 1,061,048,169 disk bytes beyond one per hash; this is not predicted
GPU savings. Lower-spec devices, long-session memory behavior and ordinary
rollout remain untested. Compatibility reports unsupported SSAO in both modes;
expected invalid-input/save warnings and the eight cleanup warnings stay visible.

All original inputs remain unchanged after execution. Of 4,787 copied runtime
files, 4,786 are byte-identical; the sole changed original is the intentionally
written disposable `game/g1/paid-home.json`. New G2 inspection files, fixtures,
imports and sidecars are accounted separately. No normal save was opened or
written, no other process interrupted and no session messaged. Hidden owned
processes use `Local\Wroughtwild-Art07-GPU` plus read-only process checks.
APPDATA is under `v01/users/<job-group>`, LOCALAPPDATA under its `local`, TEMP/TMP
under `temp`, and Blender resources under `blender-user`. Godot's retained
`ART07G1` user-dir name resolves within that G2-owned APPDATA.

## Delivery and publication

Final local review-only handoff:
`D:/project-wroughtwild-art07-g2/build/art07/g2/v02/handoff`.
Manifest SHA-256:
`49827ac3fe3522a92cb74eecdf60cd857e1157b08f5d57f2d273c617606e93a1`.
772 files / 594,208,921 bytes; every entry and the exact file set verify.

The handoff includes fresh native-resolution evidence, all logs/specs/audits,
Blender inspection assembly/renders, G2 recipes and test-only checkpoints.
`companions.json` identifies the complete unmodified `v01/sealed` sources and
`v01/review` runtime. It is not a new game/content adoption package. The receipt
stays outside the seal to avoid a self-referential hash.

A standalone reimport and smoke launch of the G2 companion can use the exact
recorded import/smoke objects with fresh log/state paths. This example is an
optional later replay, not additional evidence claimed by this receipt:

```powershell
$g2ReplayRoot='D:/project-wroughtwild-art07-g2/build/art07/g2/v03'
$g2Handoff='D:/project-wroughtwild-art07-g2/build/art07/g2/v02/handoff'
New-Item -ItemType Directory -Path $g2ReplayRoot -ErrorAction Stop
$g2Core=Get-Content -Raw "$g2Handoff/audit/core-jobs.json" | ConvertFrom-Json
$g2Render=Get-Content -Raw "$g2Handoff/audit/render-jobs.json" | ConvertFrom-Json
$g2Replay=@($g2Core[0]; ($g2Render | Where-Object id -eq 'smoke-forward_plus'))
foreach($g2Job in $g2Replay) {
  $g2Job.log="$g2ReplayRoot/logs/$($g2Job.id).log"
  $g2Job.state="$g2ReplayRoot/users/$($g2Job.id)"
}
ConvertTo-Json -InputObject $g2Replay -Depth 8 | Set-Content "$g2ReplayRoot/replay.json" -Encoding utf8
& D:/project-wroughtwild-art07-g2/tools/wroughtwild-art07/g2/run.ps1 -Spec "$g2ReplayRoot/replay.json"
```

The smoke scene verifies the paid checkpoint and exits. For manual review,
create another fresh job using `res://g1/play.tscn` without `--smoke`; normal
WASD/mouse, E, C, B/Tab/Q/R, LMB, I, H and F5/F9 controls remain. F4's fixed
inspection scene with `--play` uses E controls, W wind, F start, P pause/resume,
X cancel, K collect, G emission and Esc quit. Its mouse panel handles deposits.
For another entirely fresh source/runtime copy, follow the G2 tools README with
new version paths. Source reconstruction remains the pinned G1 archive plus its
recorded presentation hooks; canonical G1 wrappers are never redirected.

Git contains only G2 tools, 13 curated images, three motion files, checks/costs/
provenance/report and this receipt. Full sources, working builds, raw frames,
masters, engine caches and test saves remain local-only.

Checked findings commit: **`a40ff0997517a4d4688546e7553cd3e6603612d8`** on
`codex/art07-g2`. This receipt is committed separately after that findings commit.
Local commit and remote publication are separate outcomes.

Main integration and push: **not performed**. The dedicated publisher owns
serial integration/push under standing permission. No approval rejection
occurred. G2 is ready for the publisher to record this review; visual repair,
loading/residency work, target-device clearance and owner visual acceptance
remain explicit later work/decisions. No dependent slice was started.
