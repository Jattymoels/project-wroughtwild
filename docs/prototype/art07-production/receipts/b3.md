# ART-07B3 — Rock, root-bank and cave-contact kit

Status: technically delivered as source/runtime candidates and an isolated review.
Owner visual acceptance: pending. No ordinary-world adoption or publisher integration is claimed.

## Scope and lineage

- Assigned IDs: resource_nodes/boulder, resource_nodes/stone_seam; nature_support/river_bank, nature_support/talus_pebbles, nature_support/rock_shelf, nature_support/cave_threshold. No additional resource, kit, drop, recipe, geography rule or collision authority.
- Branch: `codex/art07-b3`. Worktree: `C:/Users/Matty/Dev/project-wroughtwild/build/art07/b3/worktree`.
- Game, data and freshly compiled native simulation all use the inspected published base `f00d4b76274ef2a73c3b6f4a275d2bd3ca0f958d`. No ART-07 predecessor was required. Native DLL SHA-256: `6d8094fc95c0854f9100b161806a11d9fa3a67bb4f870080976bcd8d2e8f2279`; existing godot-cpp library: `701934d2505314d4272ef258beca75974de9f2bb2d94d98b29d22670de42370c`.
- The approved ART-02 grove package was read-only: `C:/Users/Matty/Dev/project-wroughtwild/build/grove-art02/emberroot-handoff`. Its source was suitable after actual multi-angle inspection, so no new image generation or TRELLIS generation was needed.
- Verified selected source hashes: packed quiet rock master `cc8a483a7cf3f9ae5b012a8d78155ad49afb71affc2ab505078b2935f80bd61a`; root-bank GLB `0718f626d01c6d39a926cd6930db428f3d2a3bf137ec02488edae4ddec7c2b00`; immutable raw rock GLB `6185f02594ec0d064d9750fa28fe33603c513361f0a96a65512ff9668c721322`.
- Original rock concept input: `a009263af1f9b8a1bf7856212ffaa6cce08eac1f241d24ee8af529cdb7a53b2c`; retained cutout: `cc9079702437c95c6534478c23f29cfe89c381b50cc99fe5c00a17124f9cadb6`. Albedo, ORM, floor, ENV-002 reference, generator executable, Blender and install-manifest hashes are preserved in `source-provenance.json` in the evidence directory. All 13 recorded input/tool files were rehashed unchanged at final evidence collection.
- All ten pinned local model weights were checked against the install manifest. Actual source GLB metadata: trellis.cpp v0.6.0, CUDA, seed 42, build commit `16f3109e82f3922033bfa62b83c42899678b7b6f`; distinct release target `06fc9000719c912ddc4929d21db075972c26ac3e`; model revision `a57397bd3d351599d9729fc144b3f87c3f87d65b`. Original generation was 1024, GPU 0 required, retained BiRefNet cutout, PNG, eight threads, 116.2489703 seconds, exit 0. These describe the reused source, not a new B3 run.
- Existing Blender 4.5.9, Godot 4.5 stable and bundled Python/Pillow/NumPy were used; nothing installed or downloaded. B3's process runner and unchanged-current-check runner derive from the read-only B1 examples; production recipes adapt the grove approach locally. No other worker files changed.

## Implementation and review

The kit retains the source's broad fractured bedding and weathered colour, with capped boulder work cuts, two low seam shoulders around the native work ribbon, attached root-bank dressing, eight talus fragments, an ordinary/altered shelf pair, and seven modular cave-contact rocks. Ten named candidates each have three exported detail levels: 30 GLBs. The released-chunk candidate is supplied for review only; native cosmetic chips and payouts remain unchanged.

Selected geometry is kit-v09, with kit-v10 correcting the named Scar vertex-colour export. The packed master is unchanged by that export correction. Final handoff is v02; v01 lacked the separate interactive-pause verification recipe. Earlier geometry v01–v08 and review v01–v04 were diagnostic attempts, including rejected shell/UV repairs. Review-v05 corrected floor mipmaps and scar export. Review-v06 only improves the native camera and hides the player presentation from that external fixture camera. Static v05/v06 scene, shader, settings and assets are byte-identical; selected static captures and separate benchmarks remain from v05. Native v06 checks and captures were rerun.

The editable master has distinct SOURCE, FINISHED, RUNTIME and REVIEW collections. Raw and approved reduced meshes/maps remain hidden and packed. The inherited ART-02 names “Quiet river oak” and “SOURCE - intact normalized oak” are source naming residue: their actual inspected meshes are the selected rock. An unused closed-shell comparison is preserved but is not a selected near candidate.

Dimensions and transforms:

- Blender metres map once through glTF as `(x,y,z) -> Godot (x,z,-y)`. Per-host fitting uses `(vertex - source_min)/(source_max-source_min)*target_size`, recentres the horizontal bounds and anchors the bottom. This deliberately fits the existing game body; the original source is unchanged. Original reduced source dimensions were approximately 2.8323 × 2.9262 × 1.6012 Blender metres.
- Boulder target: 1.36 × 1.16 × 0.96 m in Blender XYZ; after base trimming the full export spans about 1.3448 × 1.0828 × 0.9216 m. All imported boulder states fit the existing Godot 1.4 × 1.0 × 1.2 m body, checked from actual vertices. Origin remains the native ground anchor. Existing squash/roll/depletion behaviour remains authoritative.
- Boulder stock appearances: above 6 units = full, above 3 = worked, otherwise remnant, followed by native removal at zero. Nine fieldstone remain nine; there is no persistent decorative resource remnant.
- Seam authoring envelope: 2.9 × 0.66 × 0.18 m, two bedding shoulders and an open work channel. Non-terrain fixture uses 0.88 horizontal fitting against the unchanged 2.6 × 0.6 × 0.9 m picking body. Real Terrain/SurfaceSampler reprojection retains the original GroundedSeam and its work/picking geometry. Missing or discontinuous supports remove triangles; restoring supports reconstructs exactly the original dressing vertices. No saved resource position or stock changes.
- Shelf authored envelope: 3.4 × 2.1 × 0.85 m. Bank stone: 3.5 × 1.5 × 0.46 m; root host: 3.4 × 1.4 × 0.64 m with a 0.12 m attachment offset. Talus consists of eight small source fragments without independent colliders or resource IDs.
- Cave sides are three 0.85 × 1.3 × 1.0 m modules at each side; overhang is authored at 4.1 × 1.3 × 0.65 m. Actual overall bounds are approximately 4.2805 × 1.2134 × 3.074 m. The central 2.4 m wide × 2.4 m high clearance was checked in every exported detail level and with the real player capsule through the authored retained-wall/floor fixture. The contacts require site fitting/suppression against actual retained terrain; no new cave generator or decorative collider is supplied.
- There are no new attachment sockets, material gates or construction shapes. Ordinary-world placement remains outside this candidate adapter.

Near geometry keeps the source albedo/UVs and linear ORM. Recovered planar faces have independent planar UVs. Distant geometry uses volume resampling and same-surface polygon-interpolated UV transfer, followed by restored bounds. Runtime maps are 1024 square; the master also retains original 2048-square source maps and seven packed images. All selected GLB materials are opaque; no transparent foliage layer is introduced. Actual imported surfaces, triangles, bytes, bounds and boundary edges are in model-audit.json and verification.json.

The altered shelf moves 564 source-corresponding vertices along a top-surface path. Independent packed reopen measurement finds a maximum incision of **0.06449609 m** (requested depth 0.065 m; half-width 0.11 m). Its damage is geometric and visible without emission. Scar data is a separate linear vertex channel; only the damaged shelf gets ambient moving energy. Albedo is sRGB; ORM/scar data is linear. The explicit engine clock pauses; no shader TIME, bloom or per-scar light is used. Native heat, cracking, wedge depth, work progress and depletion remain separate native state.

`tools/wroughtwild-art07/b3/kit.json` documents the purpose of every exposed slice tuning control: deterministic seed 42, boulder/seam dimensions, scar depth/width, near/mid/far targets 12000/4500/1200 triangles, review detail distances 12/28 m, pulse period 4 s and peak 2.2. These are candidate/review controls, not new game tuning or approved world budgets. Other fixed authored module sizes above describe the modelling recipe.

## Checks and evidence

Evidence root: `C:/Users/Matty/Dev/project-wroughtwild/build/art07/b3/worktree/docs/art/leyline-studies/2026-09-09/art07/b3`.
Full working root: `C:/Users/Matty/Dev/project-wroughtwild/build/art07/b3/worktree/build/art07/b3/v01`.
Selected handoff evidence: `C:/Users/Matty/Dev/project-wroughtwild/build/art07/b3/worktree/build/art07/b3/v01/handoff-v02/evidence`.
Final fresh-copy checks: `C:/Users/Matty/Dev/project-wroughtwild/build/art07/b3/worktree/build/art07/b3/v01/fresh-v02-checks`, `fresh-v02-native`, `fresh-v02-capture`, and `fresh-v02-audit.json`.

Actual executed command argument arrays, executable paths, PIDs, isolated APPDATA, elapsed seconds and exit codes are in each `*.log.job.json`. The package includes the selected production/current/native/capture/benchmark logs and the first fresh-copy verification; final v02 verification is recorded alongside the immutable package and in tracked fresh-verification.json. Principal commands, run from this worktree:

```powershell
# Existing depot compiler, explicit same game/native revision:
& C:/Users/Matty/Dev/project-wroughtwild/tools/wroughtwild-workshop/build-native.ps1 -Revision f00d4b76274ef2a73c3b6f4a275d2bd3ca0f958d -Output build/art07/b3/v01/native-v02
# Recipe paths below are tools/wroughtwild-art07/b3/, output root is build/art07/b3/v01/.
# Exact absolute program/argument arrays are in the associated job JSON files.
blender --background --threads 8 --python-exit-code 1 --python build_kit.py -- DEPOT ROOT/kit-v09 --no-render
blender --background --threads 8 --python-exit-code 1 --python render_master.py -- ROOT/kit-v09/b3-master.blend ROOT/blender-v09
blender --background --threads 8 --python-exit-code 1 --python finalize_export.py -- ROOT/kit-v09 ROOT/kit-v10
blender --background --threads 8 --python-exit-code 1 --python audit.py -- ROOT/kit-v10 ROOT/audit-v10.json
python prepare.py ROOT ROOT/kit-v10 ROOT/review-v06
godot --headless --editor --path PROJECT --import
./verify_runtime.ps1 -Root ROOT/review-v06 -Logs ROOT/checked-native-v06 -Stage Native
./run-current-checks.ps1 -Project ROOT/review-v01/native/game -Logs ROOT/current-checks
./verify_runtime.ps1 -Root ROOT/review-v05 -Logs ROOT/checked-capture-v05 -Stage Capture
./verify_runtime.ps1 -Root ROOT/review-v05 -Logs ROOT/checked-benchmark -Stage Benchmark
python encode_motion.py ROOT/review-v06 ROOT/motion
python package.py ROOT ROOT/handoff-v02
python verify_package.py ROOT/handoff-v02 ROOT/handoff-reimport-v02
blender --background --threads 8 --python-exit-code 1 --python audit.py -- ROOT/handoff-reimport-v02/models ROOT/fresh-v02-audit.json
godot --headless --path ROOT/handoff-reimport-v02/review --script res://verify_pause.gd
./verify_runtime.ps1 -Root ROOT/handoff-reimport-v02 -Logs ROOT/fresh-v02-native -Stage Native
./verify_runtime.ps1 -Root ROOT/handoff-reimport-v02 -Logs ROOT/fresh-v02-capture -Stage Capture
python verify_package.py ROOT/handoff-v02
```

The abbreviated recipe commands above state argument contracts, not executable aliases installed on this machine. The authoritative exact invocations are the job JSON files. Native v06 renderer calls were also run separately through run-job.ps1; this avoids recapturing unchanged static v05 evidence. Current checks used the copied v01 game at the same frozen commit; their source scripts and native rules are unchanged in the final copy.

Results:

- 8,225 existing assertions across 14 jobs, zero failures: unit 398; art 11; gathering 22; world 5,957; materials 158; placement 470 + fresh restore 60; building loads 201 + restore 6; loose-drop write/read 6/13; source-work 733 + restore 15; save recovery 175. Assertions were not weakened. These include failed-placement retention, exactly-one successful placement, finite source ownership, material gates and saved geography coverage.
- B3 headless native flow: 16,621 checks; partial fresh process: 10; final fresh process: 8; zero failures. The large flow count includes per-vertex terrain-support assertions. Partial save restores 6 boulder units with one drive press, 12 seam units with a half-driven wedge, 3 fieldstone and 5 remaining wedges. Final save restores 9 fieldstone, 12 split stone, zero wedges and no depleted resource nodes. Exactly six supplied fixture wedges are consumed by normal native work.
- Fresh packed Blender reopen: seven nonempty packed images; independent incision measurement; all 30 GLBs separately imported with finite coordinates and no degenerate triangles; boulder bounds and cave clearance pass. 43 selected Blender images show the kit and material/clay/back/side/top/underside views; source inspection is also retained.
- Both Forward+ and Compatibility render actual day/shade/dusk, cuts, bank/cave contacts, emission-off shelf, three detail levels, four-second scar motion, native heat/cracking/wedge/cuts and depletion. No script or shader errors in selected jobs. The separate interactive-pause test exercises Space with automatic capture mode disabled and checks every imported scar material before/after resume. The earlier missing-import attempt is diagnostic only, not a passing pause test.
- Final handoff is verified in a fresh copy: Blender reopen/import, both Godot imports, interactive pause, native flow/partial/final and both-renderer captures. Canonical manifest is rechecked afterward. User directories stay below B3's ignored output; no normal save, live DLL or owner playtest is touched.
- Blender CPU rendering explicitly used Cycles CPU and eight threads: selected build 20.05 s, 43-view render 125.41 s, scar export correction 1.22 s, separate import audit 2.35 s, all exit 0. Godot GPU work took the shared mutex and refused competing renderer/generator processes. Other workers' jobs were left alone.
- Separate settled benchmarks: RTX 5090, 1440 × 900, MSAA 4×, shadows on, VSync off, fixed camera, automatic detail, 180 warm-up frames then 600 samples. Forward+ art p50/p95/worst: 0.293/0.414/3.926 ms, 99 draws, 81,034 primitives, 453.07 MiB texture memory; hidden-art comparison 0.173/0.265/1.250 ms, 6 draws, 220 primitives, same loaded textures. Compatibility art: 0.267/0.405/0.723 ms, 99 draws, 81,042 primitives, 375.59 MiB; hidden: 0.202/0.250/1.073 ms, 6 draws, 228 primitives, same textures. These are process-frame wall intervals, not isolated GPU timestamps or display latency. Shadows are included, not isolated. No lower-spec, whole-world or texture-budget approval follows.

Limitations: near source topology retains small open edges (audit reports them), although the visible recovered cut face is capped. Distant UV detail softens; the cave modules repeat and need authored terrain intersections. The fallback native seam retains its original conspicuous work ribbon. Separately embedded maps cause substantial duplicate texture residency and need shared-material work before world adoption. The authored fixture checks real engine/native rules but supplies test inputs, uses retained-wall/floor proxies for cave passage, and does not represent every generated cave. The normal streamer does not install this adapter. The standalone review includes a raw PNG floor; compiled PCK export is not tested. Benign sandbox root-certificate warnings do not affect local rendering; no network operation was used. Owner visual acceptance remains pending.

## Delivery and publication

- Canonical handoff: `C:/Users/Matty/Dev/project-wroughtwild/build/art07/b3/worktree/build/art07/b3/v01/handoff-v02`.
- Manifest SHA-256: `b53961b58ddf4720ec51f77adfe1e87860890f4c3d9b68ec3f6de27102b39321`; 1,541 manifest entries, 693,973,744 bytes excluding the manifest itself.
- Packed source: `handoff-v02/models/b3-master.blend`; runtime GLBs in `models/`; isolated projects in `review/` and `native/game/`; native base/DLL provenance in `native/provenance.json`; reproduction and controls in `README.md` and `recipe/`.
- Launch: `& 'C:/Users/Matty/Dev/project-wroughtwild/build/art07/b3/worktree/build/art07/b3/v01/handoff-v02/recipe/launch-review.ps1' -Package 'C:/Users/Matty/Dev/project-wroughtwild/build/art07/b3/worktree/build/art07/b3/v01/handoff-v02' -Visible`. Add `-Compatibility` or `-Native`. Launcher copies into a unique sibling b3-review-session directory and isolates APPDATA in its `user/` directory. The native fixture supplies six test wedges and exits automatically. Static controls: click/mouse, WASD/QE, L lighting, M scars, Space pause, 0 auto/1–3 fixed detail, R overview, Escape cursor. Launcher is supplied; equivalent fresh-copy/import/run commands were verified, not a manual interactive playthrough.
- Exact fresh native test save directory: `C:/Users/Matty/Dev/project-wroughtwild/build/art07/b3/worktree/build/art07/b3/v01/fresh-v02-native/user/Godot/app_userdata/Wroughtwild` (actual configured app subdirectory is recorded by the copied project; all saved data remains under `fresh-v02-native/user`). Save files are fixture-only `b3-partial.json` and `b3-final.json`.
- Git owns only B3 recipes, selected actual PNG/GIF evidence, source/audit/verification JSON and this receipt. Raw generation, packed masters, GLBs, DLLs, local-only handoffs, imports, caches and saves remain ignored. A clean clone needs the transferred verified handoff or the documented existing-depot reconstruction.
- Checked implementation commit: recorded in the follow-up receipt commit after scoped staging and checks.
- Main integration and push: **not published**; the dedicated publisher integrates/pushes serially. No branch push or main checkout switch was performed here.
- B3 can be handed to the publisher and later integration review. No dependent slice is implemented; visual acceptance, normal-world placement/site fitting and texture deduplication remain separate work.
