# ART-07G1 — Retained route and home integration pilot

Status: technically delivered; sealed handoff and fresh-copy checks passed.
Owner visual acceptance: pending.

## Scope and lineage

Integration/verification only, consuming B1–B4, C1–C6, D1–D6, E1–E3 and F1–F5.
All 24 absolute source paths and manifest hashes come from the verified
[G1 input index](../g1-inputs-2026-09-12.json), SHA-256
`eb39916df348f51dae7643692becaa5340ff9bfede2ed38f5b0ecdfb21ee5fef`.
Every manifest and every listed file passed before consumption and again after
integration: **35,779 files / 19,093,094,616 bytes**, unchanged. Published receipt
commits and receipt hashes are recorded per input in `lineage/verified-after.json`.
The original sources and their generation/cutout/model lineage remain immutable.

Worktree: `C:/Users/Matty/Dev/project-wroughtwild/build/art07/g1/worktree`.
Branch: `codex/art07-g1`. Pinned current game/data/native base:
`6bb2e044dcd0bf1788896aa2c19cdf56fee93522`, matching shared main/origin/main
when inspected. Native DLL SHA-256:
`0e7c4956b6c870f5879e78f54c5830509b7ae37913055543cc3a6d451298e0e1`.
Reused godot-cpp ABI archive SHA-256:
`701934d2505314d4272ef258beca75974de9f2bb2d94d98b29d22670de42370c`.
Only the owned G1 tools, curated evidence and this receipt are intended for Git.
No normal game/data/save, queue, catalogue, accepted decision or source recipe
was edited. The shared checkout's unrelated captures, imports and saves remain.

Existing Godot 4.5-stable, Blender 4.5.9 LTS and bundled Python/Pillow were reused.
No new source generation, third-party dependency or download was needed.
All GPU jobs used `Local\Wroughtwild-Art07-GPU` and read-only process checks.
Only explicitly identified mistaken G1 launcher processes were stopped; their
exact command lines and PIDs are retained under diagnostics. No other session
was interrupted or messaged.

## Implementation and review

`prepare.py` archives the pinned game/data and installs a newly built matching
DLL. Its literal hook list and per-input copy lineage are in `integration.json`.
Owned-ID dispatch composes resource, station, building, home, device and colour
adapters. `--baseline` exposes the native art-disabled paths in the same copy.
No simulation rule, stock, inventory/work owner, material/shape gate, save schema,
collider decomposition, generator seed or geography changed.

The no-grants LF3 seed-77 pilot gathers/crafts/works via finite native sources and
ordinary panels, pays for a 6×6 m home and three usable workshop stations, and
walks the existing source route with native input/controller movement. Gathering
travel and target selection are harness-paced; this is not an uncut human pacing
claim. The home costs 188 timber and its chest retains 17 timber. Paid source
work and a Red buffer retain native quantities. Baseline/art snapshots match at
initial, paid and final stages; both measured walks are **135.078021510504 m**.
The later rendered route sequence is a separate controller run from that save.

The separate catalogue supplies explicit inspection stock and the existing roof
reward. All **273 legal shape/material pairs** retain exact payment, one placed
owner, occupied-placement refusal and original collider decomposition. This
includes every current chamfer/triangle/octagonal building use. Regional C1–C4
views use native IDs; era-2 slate/shellstone has a separate labelled native
fixture. F1–F4 devices use their existing inspection/native work cases.

Dimensions and transforms remain the predecessor/native values: the home uses
the existing metre/.5 m building grid, stations keep their original plinth/body,
the feeder retains 1.5×1.45×1.45 m bounds and its (0,.85,0) connection anchor,
and the pocket retains 1.05×1.25×1.05 m bounds. glTF already performs the Blender
Z-up to Godot Y-up conversion; no second conversion or new sockets are added.
B2 meshes are uniformly fitted inside existing decorative envelopes, preserving
native scatter positions/counts. C6 replaces existing ruin visual parts only.

F1 requests create no drive. F4's fractional escrow/exhausted state is native.
A rendered fresh-process check caught a one-frame membrane refresh delay after
restore; G1 refreshes the picture synchronously from the restored ledger before
world recovery resumes. F5 follows native claims, delivered request counters,
actual Blue timer expiry and Red paid feeder progress. Presentation never owns
stock or replays a load event. Persistent effects remain distinct from trials.

Albedo remains sRGB and normal/ORM/scar data linear. Published material maps and
geometric scars are consumed without reshaping the sources. Tunings and their
plain-language purposes are in `game/g1/settings.json`: normal strength .45,
frame shade .72, canopy/cover LOD2, F5 middle detail, rooted bend .018 and leaf
gain (.85,.72,.60). Source-specific controls remain in their published resources.
The [cost report](../../../art/leyline-studies/2026-09-09/art07/g1/costs.md)
lists each input's explicit detail choice, total loaded allocations and geometry.

Bounded visual limitations: B1's inherited narrow native fit remains visibly
pinched; distant native canopy remains outside live streaming. Generic building
forms use published face maps, with bespoke end-grain/edge roles still limited
to authored station/home assets. E3's native chest intersects a flat floor by
0.15 m. C5 raised ore remains fallback-only; normal faceted ore keeps the native
thin ribbon. B2/B4 source roles without matching native anchors remain supporting
inspection assets; no new scatter, water geography or full-width collider was
invented. Hidden resource stages/LODs and texture allocations remain costly.

## Checks and evidence

Actual [Blender/Godot renders and motion](../../../art/leyline-studies/2026-09-09/art07/g1/README.md)
are curated for chat review. The handoff contains full originals and reports.
Paid/world views are 1440×900, 4× MSAA, day/dusk in both renderers; native fixture
captures retain their 1440/1600×900 source dimensions. Blender renders are
1440×1000, Cycles CPU, 24 samples, eight threads. All 30 unchanged copied masters
reopened with all 477 file images packed. Six runtime GLBs were imported, packed
into a clearly static metric inspection assembly, reopened and rendered in
material/emission-off clay modes.

Successful current checks include:

- Units: 398; placement 470 + fresh restore 60; local scenery refresh 5,254;
  complete home/workshop 1,027; E3 775 + restore 33.
- D1 8,169 + restore 7,676; D2 6,563 + restore 6,164; D3 35,310 + restore
  40,431; catalogue 1,861 per supported renderer, all 273 legal pairs.
- E1 304 + restore 132; E2 162 + restore 12, including exact paid in-place
  upgrade, shared kit preview, body retention and failed-placement kit recovery.
- B1/B3/C1–C5 source/native/partial recovery checks; F1/F2/F3 work/ownership and
  fresh recovery; F4 152 headless checks + 30 fractional / 28 exhausted restore,
  then 227 capture checks + 32 fractional / 29 exhausted per renderer.
- Paid White 733, Blue 216, Green 129, Red 324 + fresh Red restore 14. F1's
  request/drive conservation remains covered by its native fixture assertions.
- Paired paid pilot 865 checks each, fresh home restore 8, exact baseline
  fingerprints at all stages. Final art-on/art-off probes each pass 9 checks;
  geography SHA-256 `550b2d742314136a71a8c177920059bd245996e744b8722c8ba8c68afbde4a70`,
  canonical full-snapshot SHA-256
  `2fe1ad3fa6b4581996b94b451d7d05c5d135c11281517f76f70bbf4ef1001aba`.

Only ephemeral auto-generated station scene names are omitted from cross-process
canonicalization; persistent station keys, resource IDs, positions, stock/work,
progression, structures, ownership and all other snapshot fields remain exact.

The strict runner rejected D3/catalogue dummy-renderer shutdown null-material
diagnostics despite successful assertions. Unchanged assertions passed in the
supported renderer(s); failed logs remain in `diagnostics/`. Expected native
unit rejection warnings, some source/fixture ObjectDB cleanup warnings and
Compatibility's native SSAO warning are recorded, not suppressed. Mistaken
interactive launcher attempts are not counted as successful checks.

Four uncontended benchmark processes compare four identical paid cameras ×
day/dusk in both backends, 120 warmup + 300 sampled frames per view, vsync off,
no capture/generation during timing. All cameras/targets, active resource ID
hashes and chunk counts match. Each process records exit 0 and no competitor.
On RTX 5090, art median / worst-view p95 ranges are **2.666–3.835 / 3.046–4.395 ms**
Forward+ and **4.729–6.154 / 5.802–6.916 ms** Compatibility. Full per-view baseline
comparisons remain in the JSON/table. Loaded textures are **1,307.8 / 1,288.5 MiB**;
live unique geometry is **2,420,221–2,503,669 triangles**, including hidden
work stages and LODs. Buffer/video/vertex/index/surface/instance totals and
measurement definitions are reported separately. No lower-end hardware or
owner performance/visual acceptance is claimed.

Exact commands and full executable arguments/exit codes are recorded in
`evidence/validation.json` and paired `.log.json` receipts. Reproduction entry
points are in `tools/wroughtwild-art07/g1/README.md` and the packaged recipes:
`verify_inputs.py`, `build-native.ps1`, `prepare.py`, `checks.ps1`,
`final-captures.ps1`, `render.ps1`, `probe_compare.py`, `benchmark_compare.py`,
`stage_masters.py`, `reopen.py`, `fresh_copy.py`, `package.py`, `manifest.py`.

## Delivery and publication

Absolute handoff:
`C:/Users/Matty/Dev/project-wroughtwild/build/art07/g1/worktree/build/art07/g1/v06/handoff`.
Manifest SHA-256:
`fd5c592ef52185cc7d0737840e41af09dbb5bcea36b719539931e156f42865cd`.
The seal covers **5,701 files / 7,430,015,212 bytes**, excluding only documented
generated caches, UID sidecars and Blender backup files. Every sealed file was
rehashed after packaging. `evidence/validation.json` records **81 successful job
receipts**, with failed diagnostics excluded.

Fresh-copy import/launch verification passed at
`C:/Users/Matty/Dev/project-wroughtwild/build/art07/g1/worktree/build/art07/g1/v06/fresh`.
All 4,787 runtime files / 3,438,257,458 bytes matched before import and remained
unchanged after import. The included engine freshly imported the project, passed
398 unit checks and printed `G1_PACKAGED_LAUNCH_OK` in both Forward+ and
Compatibility. All four jobs exited 0; the only renderer warning is the native
Compatibility SSAO limitation. These launches used the new isolated
`v06/fresh-user/ART07G1` directory, without a pre-existing play save.

The standalone package includes the existing Godot executable, matching DLL,
game/data, paid checkpoint, 30 packed masters, recipes, source manifests/receipts,
actual images/motion frames, matched benchmarks, validation and failed diagnostics.
From the package: `./launch.ps1 -Import`, then `./launch.ps1`;
`-Renderer gl_compatibility`, `-Baseline` and `-Smoke` select the checked modes.
Saves use sibling `handoff-user/ART07G1/g1-play.json`, never the normal save path.
Controls remain WASD/mouse, E, C, B/Tab/Q/R, LMB, I, H and F5/F9.
Verify sealed bytes with `python recipes/g1/manifest.py verify <absolute-package>`.

Checked implementation and curated evidence commit:
`1d3d3ce29c4afe7e1db970b880953e92035a0b7b` on `codex/art07-g1`.
This receipt is recorded in the subsequent receipt-only commit, whose exact SHA
is supplied in the final handback. The sealed recipes retain the checked run's
bytes; Git sources only trim trailing blank lines in `build-native.ps1`,
`gpu-slot.ps1` and `paid.gd`. No executable statement changed after validation.
Main integration and push: **not published; dedicated publisher owns this step**.
G1 technical handoff only. **G2 has not started. Owner visual acceptance remains pending.**

## Publisher integration — 13 September 2026

The two exact worker commits above were fast-forwarded to main without rewriting
their identities: implementation `1d3d3ce29c4afe7e1db970b880953e92035a0b7b`, receipt
`4412781c70ce8cbc7651f9262df6831593b83874`. The ordinary source push succeeded.
The [seventh publication report](../publication-2026-09-13.md) records independent
fresh-package checks, canonical hashes, paid baseline equivalence, packed-source
reopen and both-renderer recovery evidence. Sealed package and worker recipe/media
bytes are unchanged. Main checked-out text has Git's newline normalization in
31 files; executable statements and committed Git blobs are unchanged. The
publication report records both raw hashes. This appendix records publication only.

G1 is technically delivered: 25 of 26 dispatch slices. G2 is ready for a separate
review session that did not implement G1. Owner visual acceptance and normal-world
rollout remain separate. The narrow canopy fit, generic material joins, chest
seating, fallback-only raised ore and combined texture/geometry costs remain
explicit review concerns; technical publication does not accept these visually.
