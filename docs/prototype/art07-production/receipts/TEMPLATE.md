# ART-07 [ID] — [title]

Status: not started / source candidate / technically delivered / needs repair.
Owner visual acceptance: pending / exact later response and date.

## Scope and lineage

- Assigned catalogue IDs and explicit omissions:
- Worktree/branch and exact base game/native commit:
- Prerequisite receipt/commit/package hashes actually consumed:
- Original concept/input/raw source hashes; selected source version:
- Existing local tools and actual runtime/model metadata:

## Implementation and review

- What changed and how it serves the brief:
- Actual model vs concept, including remaining weaknesses:
- Dimensions, raw-to-game transform, pivots, attachment sockets and body fit:
- Materials/UVs/linear maps, real scar depth and pulse authority:
- LODs/triangles/surfaces/textures and documented controls/purposes:
- Native state or explicitly static review limitations:

## Checks and evidence

- Exact commands, exit codes and result/log paths:
- Blender packed-source reopen and fresh GLB imports:
- Actual Forward+ and Compatibility images/video, dark/lit and relevant states:
- Resource/device ownership, failed placement and fresh-process reload where relevant:
- Performance conditions, measured results and untested devices/cases:
- Original hashes unchanged, no normal-save/live-process changes:

## Delivery and publication

- Absolute local handoff path and manifest SHA-256:
- Standalone launch/reimport command, isolated save path and source reconstruction:
- Files intended for Git vs local-only raw/build/master outputs:
- Checked branch commit SHA(s):
- Main integration and push: publisher records separately; not yet published.
- Ready dependent slice(s) and remaining blockers:

Embed actual model evidence in the session's final response, not only file links.
