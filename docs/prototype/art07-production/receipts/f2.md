# ART-07F2 — Thrumroot winch and landing

Status: technically delivered.
Owner visual acceptance: pending.

## Scope and lineage

- Assigned `resource_nodes/thrumroot`, `devices/cargo_winch` and
  `devices/winch_landing`, including one basket and a recovered-root sample.
  No F4, normal-game integration, shared queue/catalogue or other slice edits.
- Worktree: `C:/Users/Matty/Dev/project-wroughtwild/build/art07/f2/worktree`;
  branch `codex/art07-f2`. Exact frozen game/native and published starting base:
  `4b5d89b376765fbf4d46049aa099e0bb154a82da`. The owner's dirty main checkout,
  saves and playtests were preserved. Remote origin was verified read-only.
- D4 source commit `d1ae974521760cfe2c3d3e379bc34564ae93ab45`, published
  `fd1a17d` / `b1abc42`. Consumed
  `C:/Users/Matty/Dev/project-wroughtwild-art07-d4/build/art07/d4/h04`;
  manifest SHA-256
  `aaa0316f108cdb1de0ed27cc3a40fac7a07574497c14486e88b18fa73dfbda59`.
  All 196 manifest files verified before and after.
- D6 source `20c5e0b4d663037e8573488e895ba9fb882fea74`, published
  `b7116c733deb971402af6245c11a9fe15c50063b` /
  `3bc4f6844fc0adf7b9cb00c93852b9dd81de4222`. Consumed
  `C:/Users/Matty/Dev/project-wroughtwild/build/art07/d6/worktree/build/art07/d6/v04/handoff`;
  manifest SHA-256
  `8d82eb36775959f41905e579e36d737caa4a8512ed504d679ab49652301338a7`.
  All 78 files verified before and after; both predecessor receipts exist at base.
- Existing source meshes were inspected in actual Blender views. Their primitive
  source form was unsuitable, so a single-object image was generated; the precise
  timber/iron mechanisms were directly modeled. Input `thrumroot-input-v01.png`
  SHA-256 `6464b4fae4bcf3fd8e363ba68eaacf90e5b4a9db012e017074af7368e0c6fca1`;
  exact prompt tracked beside it. Raw v01 GLB SHA-256
  `23a1e87b9994d0790fc4147594329e8dcd96a29c9ae5a5c0e53df05a59578842`.
- Installed Windows CUDA TRELLIS v0.6.0; all ten pinned model hashes verified,
  model revision `a57397bd3d351599d9729fc144b3f87c3f87d65b`, 1024, seed 42,
  GPU 0 required, BiRefNet retained cutout, PNG, eight threads. Actual GLB metadata
  identifies commit `16f3109e82f3922033bfa62b83c42899678b7b6f`, CUDA build
  `2026-08-21T08:39:09Z`; this differs from the release manifest's target commit
  `06fc9000719c912ddc4929d21db075972c26ac3e` and is reported as observed.
  Generation exit 0, 112.409 seconds; 291,900 raw triangles, zero degenerate or
  nonfinite geometry. No new downloads or third-party dependencies.
- Blender 4.5.9; Godot 4.5 stable `876b29033`; existing MinGW/native ABI build.
  Fresh frozen DLL SHA-256
  `3cfc50f58319dc7e22bc197343c98e1beb16204b791633c45f9650b67bb4b173`.

## Implementation and review

- Selected source: corrected inspection v02 → carved/reduced source v03.
  Selected mechanisms: devices v03. Timber grain follows member length; end faces
  use D4 end grain. D6 iron appears on bearings, collars, guides, crank and basket.
  Root winding has woody lobes, attached spurs and recessed living tissue;
  the ordinary landing has an empty cradle and no duplicated rare core.
- The package includes packed sources, seven GLBs, source albedo, source and
  device shaders, and an isolated native study. Only the copied presentation
  adapter and review scene differ from base. Native simulation, tuning,
  `ContraptionSite`, `ResourceNode`, transactions and save code are unchanged.
- Native body: both endpoints 1.4 × 1.83 × 1.15 m, ground-centred; cable sockets
  (0,1.7,0); immediate `Drum` child pivot (0,1.05,0), axis X. Native rotation
  `(energy*.45 + progress*3.0)*TAU`. Basket origin cable minus .45 m; all exported
  vertices fit native .3 m swept sphere centred cable minus .25 m.
  Final device visual envelopes are approximately 1.32 × 1.8175 × 1.02 and
  1.26 × 1.8175 × 1.02 m. Source near GLB is 2.13904 × .79047 × .71448 m inside
  the original 2.2 × .9 × .75 m body.
- Raw fitting uses +90° Blender Z, uniform scale 2.1364885987864177 and the
  measured origin translation. A documented depth-only finishing factor
  .5364793037157609 fits the generated 1.323 m depth into the native body.
  Source grooves are actual surface-following cuts: .046 m influence radius,
  .026 m requested recession; median sampled depth .0224118 m, maximum .0257534 m.
  Winding/recovered sections recess .012/.014 m. Cuts remain with emission off.
- Source LODs: 64,000 / 26,000 / 9,000 triangles, one surface each. Winch 22,360
  triangles / 7 surfaces; landing 3,716 / 3; basket 3,848 / 3; recovered coil
  7,708 / 2. Every final GLB independently imports with zero degenerate or
  nonfinite geometry. The study uses near source; middle/far are supplied for F4.
- Albedo sRGB, normal/ORM linear, mipmaps and metric grain. Source vertex masks
  retain continuous dark margins after reduction. Moving-light shader has no
  `TIME`: accepted native winding and paid progress drive it. Physical blocking
  or review pause freezes pulse phase and mechanics. Idle tissue response is
  faint and static. Controls and every introduced art parameter are explained in
  [CONTRACTS.md](../../../../tools/wroughtwild-art07/f2/CONTRACTS.md).
- Real pair cost: **14 wood, 3 iron_ingot, one Thrumroot**. Drum: 8/2/1;
  landing: 6/1/0. One wind stores one operation; each trip including empty return
  spends one. Cargo capacity 96; native 3 m/s; study span 8 m. Only the drum owns
  cargo; landing forwards withdrawals. Landing removal refunds 3 wood and recalls
  the same basket; drum removal refunds 4 wood, 1 iron, 1 intact root and all cargo,
  venting stored winding. No duplicate payment, ownership or new save enums.
- Source remains finite: four accepted work presses release exactly three native
  pickups. Partial progress and depleted state survive fresh-process saves.
  The recovered coil on the inspection ground is a labelled static sample when
  the pack owns roots; it is not a second pickup or inventory owner.
- Rejected iterations are retained locally: existing primitive source, wrong raw
  long-axis fit, coarse scar face colouring, unsupported top guide, oversized
  basket, erased empty-node pivot, and Blender's suffixed static mesh name.
  Pivot and placement assertions caught the latter defects; no assertions were
  weakened. A first diagnostic reopen returned exit 0 with a Python traceback;
  it was rejected and `--python-exit-code 1` plus traceback detection enforced.

## Checks and evidence

All relative paths in this section are under the absolute worktree above.
Exact executable paths, arguments, PIDs, durations, exits and isolated save roots
are in the corresponding log JSON; reconstruction commands are in
[the recipe README](../../../../tools/wroughtwild-art07/f2/README.md).

| Checked run | Result and evidence |
| --- | --- |
| Frozen native build | Exit 0; `build/art07/f2/v01/native/provenance.json` |
| Unchanged base regression before skin | 470 placement + 60 fresh restart + 98 station + 85 contraption checks, 0 failures; `v01/regression` |
| Final unchanged regression with skins | Same 713 checks, 0 failures; `build/art07/f2/v03/regression02` |
| F2 paid behaviour | 50 checks, 0 failures; `build/art07/f2/v03/checks02/check.*` |
| F2 fresh-process saves | 29 checks, 0 failures; `checks02/restart.*` |
| Packed-source reopen / seven GLB imports | Exit 0; `build/art07/f2/v03/reopen-final/reopen.json` |
| Forward+ / Compatibility captures | Each 16 setup checks, 0 failures; `v03/captures02` |
| Route and close motion in both renderers | Each 17 checks, 0 failures; `v03/motion02` and `v03/detail`; 108 real frames each |
| Independent motion-data audit | 20 cargo throughout, zero stored energy after payment, one arrival; frames 24–47 hold exact state and poses at .375 progress; close and wide native records match |
| Original input and predecessor hashes | Before/after records identical; `build/art07/f2/inputs-after.json` |
| Frozen-copy scope audit | 1,054 base files unchanged; only copied project settings and art adapter edited; regenerated `.import` metadata excluded; `build/art07/f2/scope-audit.json` |

The tests cover supported endpoints, real span obstruction, ordinary paid
placement and kit retention on failure, one scene/owner, paid winding/trips,
partial collection, endpoint removal, partial source saves, mid-trip disk save
and separate-process restore, no duplicated basket/inventory or phantom motion.
Current material/shape gates and saved geography checks remain unchanged.

Actual Blender/Godot PNGs, emission-off and shade states, route motion and close
drum motion are tracked in
`docs/art/leyline-studies/2026-09-09/art07/f2/`; its `evidence.json` hashes the
media and source frames. These are rendered models, not the input illustration.
All 24 blocked frames are also byte-identical PNGs in each renderer. Motion playback is 42 ms per frame (approximately 24 Hz) with no synthesized
frames. Native sampling is exactly 1/24 second.

Separate benchmark processes ran on NVIDIA GeForce RTX 5090 after the shared
slot became free, with no other art/Godot process at launch. Each used 1440×900,
4× MSAA, VSync off, 120 warmup frames and 600 measured process-frame intervals.
These are local engine loop intervals, not GPU timestamp queries or full-game
performance guarantees. Both processes exited 0; commands/logs are in
`build/art07/f2/v03/benchmark02` and measured JSON is in the gallery.

| Renderer | p50 / p95 / worst ms | Draws / primitives | Texture bytes |
| --- | --- | --- | --- |
| Forward+ | .444 / 1.018 / 2.163 | 97 / 409,846 | 375,075,328 |
| Compatibility | .581 / .664 / 1.105 | 97 / 409,846 | 231,323,982 |

No low-end, integrated GPU, long-session memory or many-device stress benchmark
was performed. The first six-minute queue expired without launching; it was
requeued without touching other work. Successful benchmark processes ran at
13:46:38 and 13:46:42 Adelaide time on 11 September 2026, separately from capture
and generation.

The sealed handoff was hash-verified, copied to
`build/art07/f2/v04/validation-copy`, and cold-imported by Godot in 13.092 seconds
(exit 0). That copy passed 50 behaviour and 29 separate-process restore checks.
Its packed masters were independently reopened and rendered, and all seven
GLBs freshly imported in Blender (exit 0, 8.221 seconds). The corrected framing
shows the complete assembled drum/basket. Exact commands, packed-image results,
geometry and the post-import unchanged manifest proof are tracked in
`docs/art/leyline-studies/2026-09-09/art07/f2/handoff-validation.json`.
Fresh logs: `build/art07/f2/v04/fresh-checks`, `fresh-reopen` and
`fresh-reopen.log.json`. All 1,830 sealed files still match their hashes after
validation. No canonical package import cache or save was created.

No normal saves changed. Test saves are under this worktree's ignored
`build/art07/f2/u/F2Native` and `nu/F2Native`. Jobs used the shared ART-07 GPU
mutex and read-only process checks. No other workers were interrupted or messaged.
One timed-out F2 diagnostic engine child was identified by exact parent/path and
stopped; the timeout wrapper now handles its own console/engine pair. Sandbox
certificate-store access required ordinary escalation; no automatic approval
review rejection occurred. Some isolated setup/capture runs report an ObjectDB
shutdown leak warning; there are no remaining engine/script errors in selected
successful runs, and all assertions pass. No runtime leak fix is claimed.

Limitations: source close-up softness and depth compression, regular recovered
coil silhouette, no automatic source-LOD selection in this study, native winding
angle snap, no new persistent depleted stump or hand animation. This controlled
inspection supplies stock explicitly; it makes no first-hour pacing, normal-world
art adoption or full-game performance claim. Cargo content is shown by the
native aggregate count, without per-item meshes inside the basket. Owner visual
acceptance is pending.

## Delivery and publication

- Verified sealed handoff, 1,830 files / 808,257,227 bytes excluding its manifest:
  `C:/Users/Matty/Dev/project-wroughtwild/build/art07/f2/worktree/build/art07/f2/v04/handoff`.
  `manifest.json` SHA-256:
  **`0a182b3cb25c5815497cf79645ff1be7769cf1d9d012572bfbbb0a8116d0d9cd`**.
- Launch a fresh copy with `./launch.ps1` or
  `./launch.ps1 -Renderer gl_compatibility`; it imports the frozen review game and
  isolates saves under that copy's `isolated-user/F2Native`. Keys 1–4 select views;
  buttons work, wind, load, request, collect, obstruct and pause; G emission off;
  R restores the paid inspection checkpoint; Esc closes.
- Git: only owned recipes/contracts, source input, curated media/hashes and this
  receipt. Local only: raw TRELLIS outputs, packed masters, native DLL/build,
  full frame sequences, frozen game, test saves and sealed handoff.
- Checked branch commit SHA(s): recorded in the follow-up receipt appendix.
- Main integration and push: **not performed**; dedicated publisher owns both.
- Ready dependent slice after publication: F4. F2 does not start it.

The tracked validation sidecar and reopened-package image were produced after
sealing; they validate the immutable package rather than changing its manifest.
Scoped Git attributes preserve LF text so evidence hashes survive checkout.
The early duplicate diagnostic image under `C:/build/art07/f2/v01/existing`
was removed only after matching its hash to the retained worktree image.

## Checked commit appendix

Checked implementation/evidence commit:
**`1ec2fcc536013c482292366d31aeb5fe60b44b11`** on `codex/art07-f2`.
The worktree was clean after that commit. This receipt-only follow-up records
the immutable implementation SHA; its own exact SHA is reported in the session
result. No source, native state, package bytes or evidence changed in this
follow-up. Main integration and remote push remain **not performed**.

## Publisher integration — 11 September 2026

The exact worker commits above were integrated without source edits as `025fd5cf905b5c761d6354d45ecb12fe6d02cf0d`, `637b4a73d4ef74c75378a7a623a3a4af357ffa2c`.
Canonical manifests and independent fresh publisher checks pass. The
[fifth publication report](../publication-2026-09-11-batch5.md) records packed
source, engine captures/motion, native state/restart evidence and remaining
limits. This is a technical source handoff; owner visual acceptance and
ordinary-world adoption remain separate. The canonical package is unchanged.
